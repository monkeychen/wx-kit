# wx-kit v0.12.0 产品需求文档（迭代 PRD）

> 状态：**开发完成，尚未发布**（2026-09-21 首稿；M76–M78 增补至 2026-09-24）。
> 本文是 M67–M78 的产品范围与验收契约；方案依据见
> `docs/superpowers/specs/2026-09-20-topic-decisions-design.md`，实现步骤见对应 `docs/plans/`。
> 当前发布版仍为 v0.11.3，根包版本在正式发版前不提前修改。

## 1. 一句话定义

**把本地文库变成一个可解释的选题工作台，帮助已有素材但不知道写什么的创作者，更快决定下一篇写什么、为什么值得写、从哪里下笔。**

## 2. 用户问题与成功标准

### 2.1 目标用户

持续写公众号、网站或其它图文内容的个人创作者，已经有收集素材和调用大模型的习惯，
但缺少稳定的选题判断过程。首版面向普通用户，不要求理解提示词、向量库或 Agent 编排。

### 2.2 核心问题

用户面对一批已收集的信息时，不知道：

- 哪个问题现在值得写；
- 读者可能获得知识、信息差、情绪共鸣、缓解焦虑或快乐中的哪些价值；
- 推荐理由依据了哪些本地材料；
- 哪些只是编辑推断，哪些事实仍需补充验证；
- 文章第一步可以从哪里下手。

### 2.3 首版闭环

本地文库 → 用户选择时间范围 → 最多三张可解释选题卡 → 用户主动选择 →
查看理由、材料与起笔结构 → 记录反馈或生成选题简报。

系统不替用户自动选题，不根据模型自评分承诺平台推流、阅读量或“爆款概率”。

### 2.4 成功标准

工程完成不等于产品有效。v0.12.0 的成功分两层：

1. **工程层**：同一份本地材料可经 GUI/CLI 得到可追溯、可复算、失败不伪装的候选；
   密钥和正文边界可核验。
2. **产品层**：在真实自有文库中，用户能从候选中找到至少一个愿意继续判断或动笔的题目，
   并认为它减少了“翻素材却不知道写什么”的时间。该层必须通过真实使用记录验证，不能由
   fixture、模型自评或测试数量代替。

## 3. 产品需求

### R1 · 用户指定输入素材

- **M78 起 GUI 只提供「人工选择文章」**：不再提供时间范围/自定义日期选择，工具栏为
  「输入素材」+ 选文章入口。时间窗口能力仅 CLI 保留（`--days`/`--from/--to`）。
- 时间范围只按原文真实发表时间筛选，不用下载时间、目录名或订阅检查时间替代。
- 手动选篇按用户指名即素材：不做发表时间纳入/排除判定；文库中找不到的 ID 显式失败，不静默丢弃。
- 用户点击分析后，本轮素材固定；素材不足时如实提示。
- 只有日期、没有具体时刻的旧材料保留时间精度；与范围边界部分重叠时不猜测具体时刻。
- 单轮最多处理 30 篇有效文章和 120,000 个去重正文字符；超限要求用户缩小范围，不静默截断。
- **选题历史（M78）**：每次分析本就按 run 落盘（结果/素材快照/反馈/简报）；GUI 提供历史列表
  （时间倒序，含状态与候选标题）、回看（含反馈状态，简报/反馈在回看中继续可用）与删除——
  「仅从列表移除」只隐藏 UI 记录（localStorage），「删除记录和本地文件」物理清除该 run 的
  全部落盘文件且不可恢复。
- **选稿弹层行内两个入口（M78）**：「看一眼」优先展示作者自写摘要，缺失才懒加载正文
  开头截断（不预取全量正文，结果缓存）；「查看原文」跳文库阅读器复用既有渲染，返回选稿
  时弹层自动重开、已选与筛选保留。

### R2 · 候选必须可解释、可追溯

- 单轮最多交付三张候选卡；允许合法返回零张。
- 每张卡包含：题目问题、写作角度、读者价值、推荐理由、材料依据、依据把握、限制、
  待补证据和起笔结构。
- 材料摘录必须能在本轮不可变快照的编号段落中逐字定位；匹配只证明摘录存在，
  不等于外部事实已经验证。
- 文章数、账号数、内容组数和发表日期由程序重算，模型不得直接提供最终统计。
- 完全相同的正文合并为一个内容组，但保留原文章身份；账号数量不得称为“独立事实来源”。
- 读者价值和推荐理由明确属于编辑推断；传播效果固定为“未验证”。
- 模型结构错误、坏引用和部分坏卡必须保留失败类型，不能伪装成“今天没有值得写的”。

### R3 · GUI 为主要普通用户入口

- 主导航新增“选题”，页面标题为“今天写什么”。
- 三张候选平等展示，分析完成后默认不选中任何一张，不用排序或视觉权重暗示“系统替你决定”。
- 用户点选候选后，同页按“为什么值得写 / 材料依据 / 怎么下笔”渐进展开详情。
- 材料页显示来源标题、账号、逐字摘录和 HTTPS 原文入口，并提示“摘录存在不等于事实已验证”。
- 阅读器返回按钮按进入来源回跳（选稿弹层 / 订阅 / 下载 / 文库），文案随来源变化，
  不把所有入口都扔回文库列表（M78）。
- 分析中显示当前阶段并允许取消；材料不足、完成空结果、部分成功、取消和失败使用不同反馈。
- 未配置模型时给出“前往设置”的可行动入口，不发模型请求。

### R4 · CLI 为 agent 自动化入口

- `wx-kit topics analyze` 支持 `24h/3d/7d/custom`，或 `--article <id>`（可重复/逗号分隔，与时间参数互斥，M75）；stdout 保持单行 JSON，stderr 输出阶段进度。
- `wx-kit topics brief --run <runId> --topic <topicId>` 读取本地结果生成 Markdown，
  不再次调用模型。
- CLI 与 GUI 共享 `src/core/topics/` 的时间、快照、校验、统计、状态和简报逻辑，
  不维护第二套提示词或判断规则。
- CLI Key 只从 `WXKIT_AI_API_KEY` 读取，不提供 `--api-key`，避免进入 shell 历史。

### R5 · BYOK 与数据出机边界

- 首版只承诺 OpenAI Chat Completions 兼容协议，不承诺“兼容所有模型供应商”。
- GUI 在“设置 → AI 模型”保存厂商、计费模式、模型、推理偏好和 Key（M73 升级为多厂商设置，端点由厂商目录自动确定，自定义厂商时手填 base URL）；分析页与设置页都明确提示：
  所选文章正文会发送到用户配置的外部服务。
- base URL 和 model 可进入普通设置；Key 不得进入 `settings.json`、renderer 可读状态、
  stdout、result、trace 或诊断日志。
- 系统加密可用时，Key 用 Electron `safeStorage` 加密后独立以 0600 权限原子落盘；
  不可用时只保留当前进程内存并显示“仅本次会话，重启需重填”，不得降级明文。
- 本地 manifest 可在文库私有运行目录保存本轮正文快照；result/CLI 输出/简报只保留通过校验的
  必要证据摘录，不复制整篇正文；trace 与诊断日志不记录正文或模型请求体。
- 模型通道诊断只记录脱敏后的端点、模型、阶段、状态和耗时；自动重试为零，避免重复计费。

### R6 · 反馈与选题简报

- 用户可对选中候选记录“暂不写 / 保存观察 / 已经写过”。每次反馈独立落事件文件，
  不把一次“暂不写”升级成永久主题偏好。
- 用户可生成确定性的 Markdown 选题简报；生成后自动复制，并可在 Finder 中显示文件。
- 切换卡片、切换详情页签、反馈和生成简报均不得再次调用模型。
- 运行产物保存在 `<文库根>/topic-decisions/`；真实评估产物和用户文库不得提交仓库。

### R7 · 状态与失败语义

- `completed`：分析完成，允许零候选。
- `insufficient-material`：所选范围没有可分析文字材料。
- `partial`：至少一张候选有效，同时有提取项或候选未通过校验。
- `cancelled`：用户取消，不包装成请求失败或空结果。
- `failed`：供应商、校验或存储失败，保留结构化错误。
- 同一时刻只允许一个 GUI 分析；第二次请求引导用户等待或先取消当前分析。

### R8 · 设置页信息架构（M71–M72）

- 微信请求保护、微信读书账号、墨问集成等状态与操作继续属于设置页，
  不迁移到下载、订阅或选题页。
- 桌面宽度下使用左侧分类导航，右侧一次只展示一类设置；窄宽度可退化为横向可滚动的分类导航。
- 固定五类：
  1. 文库与下载：文库位置、默认格式、文中视频、下载历史；
  2. 账号与平台：微信读书账号、墨问集成；
  3. 自动化与发布：订阅检查、站点同步；
  4. AI 与工具：AI 模型、命令行快捷方式；
  5. 系统与支持：微信请求保护、诊断、关于与更新。
- 每类顶部先给三张状态摘要卡，详细设置再按关联性归入独立的分组 surface；不再把十三个区块平铺成一个大卡片或一条长页面。
- 以已确认原型 `.superpowers/brainstorm/56694-1789954698/content/settings-left-nav-v1.html` 为视觉与布局契约：桌面容器宽 1420px、左侧栏 248px、两栏间距 22px；左侧栏为 12px 圆角独立卡片，五项导航各自含图标、两行说明和状态点。
- 右侧分类标题保留功能性描述，页面级副标题使用原型文案“所有账户、服务与偏好仍在这里，通过分类降低寻找成本。”。
- 每个分组卡使用 12px 圆角、独立阴影和 14px 下间距；卡内行为“左侧说明 + 右侧控件”的两列布局，左列 180–260px，控件右对齐。
- 切换分类不丢失未保存的表单值。持久设置统一由右侧底部粘性保存条提交，
  明确显示“未修改 / 有未保存更改”并支持撤销未保存修改。
- 登录/退出、清空历史、重建索引、重新检测、暂停请求、检查更新等即时动作仍使用各自按钮和确认流程，
  不伪装成需要“保存”的表单字段。

## 3.5 非目标

- 不生成全文，不改写用户已有写作流程。
- 不做配图、微信公众号排版、多平台发布、引流交易、支付、会员或佣金结算。
- 不抓全网热点，不把本地素材数量称为全网热度。
- 不接平台阅读、点赞或推荐数据，不预测阅读量或推流概率。
- 不做后台监控、自动选题、自动扩窗或“自上次分析以来”的隐式水位。
- 不复活旧 `wx-kit-compose`，不为选题维护与 core 并行的另一套工作流。
- 不在 v0.12.0 同时解决商业化闭环；本版先验证选题效率是否真实存在。

## 4. 验收清单（逐条）

### 4.1 时间、快照与证据核心（M67–M68）

- [x] 24h/3d/7d/custom 按北京时间和真实发表时间筛选，覆盖跨年、闰日、边界毫秒、
  日期精度和非法输入。
- [x] 十二类合成评估材料覆盖同源转载、旧事新发、单来源事实、观点冲突、残缺正文、
  纯媒体、重复角度和材料内指令注入。
- [x] 快照做路径边界、正文质量、完全重复分组和 30 篇/12 万字符护栏。
- [x] 模型返回按 unknown 处理；段落、逐字摘录、证据引用、卡片数量和最终统计均经运行时校验。
- [x] manifest/result/trace/brief 私有落盘；失败状态互相区分。

### 4.2 CLI 与模型协议（M69）

- [x] `topics analyze/brief` 进入 CLI 白名单，不会误开 GUI 后挂起。
- [x] Chat Completions 本地 HTTP fixture 验证请求路径、Bearer、严格 JSON、usage、超时、取消和零重试。
- [x] CLI help 与 agent Skill 明确正文会发送外部服务，Key 仅从环境变量读取。
- [x] 测试 Key 不进入 stdout、result、trace 或诊断日志。

### 4.3 GUI、安全存储与完整闭环（M70）

- [x] 设置页支持保存/清除配置，并区分“已安全保存”和“仅本次会话”。
- [x] Key 加密文件不含明文、权限 0600；普通 settings 不含 Key；损坏密文给可行动错误；
  会话 Key 替换时删除旧密文，避免重启后复活旧 Key。
- [x] 选题页面支持四种范围、三卡平等、默认未选、详情三页签、取消和不同结果状态。
- [x] 反馈独立事件落盘；简报复制并提供显示文件；两者均不增加模型请求。
- [x] Electron fixture e2e 走真实 renderer → preload → IPC → core → 本地 HTTP 模型闭环，
  恰好两次模型请求；Key、Authorization 和整篇正文在普通设置/result/trace/main.log 中零命中。
- [x] 全量 93 个测试文件、902 个单测，lint 0 错误、TypeScript 检查和 Electron e2e 通过
  （2026-09-20）。

### 4.4 设置页信息架构（M71）

- [x] 五个分类导航完整可用，默认进入“文库与下载”，右侧只显示当前分类。
- [x] 现有十三个设置/状态区块按 R8 完整归类，没有功能丢失或迁出设置页。
- [x] 切换分类后表单草稿仍在；粘性保存条能识别 dirty/clean，保存普通设置和选题 AI 公开配置/Key，撤销恢复上次已保存值。
- [x] 即时动作仍按原有安全边界执行；分类切换和统一保存不增加网络请求或改变订阅/下载逻辑。
- [x] Electron e2e 覆盖分类导航、可见性、跨分类草稿、统一保存、刷新持久和既有设置动作入口。
- [x] 设置视图纯逻辑 7 个新用例；全量 94 个测试文件、909 个单测，lint 0 错误、TypeScript 检查和 Electron e2e 通过（2026-09-21）。

### 4.5 设置页原型保真（M72）

- [x] 标题、页面级副标题、两栏尺寸、侧栏卡片、导航项层级、三张摘要卡、分组卡和粘性保存条与已确认原型一致。
- [x] 五个分类的所有现有动作仍可用，并在其原型对应的分组卡和行布局中。
- [x] Electron e2e 断言分组卡结构、页面级副标题和四个以上的分类跳转；桌面截图人工比对原型（2026-09-21）。

### 4.6 发布前尚未完成

- [x] 使用安哥自己的真实文库和明确选择的模型服务完成至少一次 GUI 端到端人工验收，
  记录候选是否实际可用、引用是否足以支撑动笔，以及失败或空结果是否可理解。
  （2026-09-25 安哥确认完成：真实文库多轮选题使用贯穿 M74–M78 的全部交互反馈来源，
  Windows 首跑暴露并修复 wxfile 回放侧图片 bug。）
- [x] 根据真实使用结果决定是否需要调整提示规则或交互；不得用 fixture 结果宣称语义质量通过。
  （M74 提示词协议自含、M76 契约一等公民、M75 流式与取消、M77 筛选改版、M78 输入
  素材收敛与历史选题，均来自真实使用反馈并已落地。）
- [x] 完成 v0.12.0 发版检查：版本 bump、README/用户手册与真实 GUI 截图、发布说明、
  三平台构建和本地打包产物验收、GitHub Release + brew tap 核实。
  （2026-09-25：三平台产物真机验证通过；GitHub Release isDraft=false 且为 Latest、
  三资产 sha256 与本地逐字节一致；brew tap cask 语法/version/sha256/url 零下载核实通过。）

### 4.7 多厂商 AI 模型设置（M73）

- [x] 厂商目录为唯一真相源：7 厂商（智谱/千问/DeepSeek/Kimi/MiniMax/OpenAI/Google）+ 自定义；
  每厂商的端点、计费模式、模型候选与推理能力声明在 `src/core/topics/providers.ts`。
- [x] 非“自定义”厂商的 base URL 由服务端按厂商+计费模式派生，不信任客户端传入的 URL；
  自定义厂商必须手填 base URL。
- [x] 推理参数只对有文档依据的厂商下发：智谱 `thinking.type`、千问 `enable_thinking`、
  OpenAI/Google `reasoning_effort`（等级五档 low/medium/high/extra/max）；DeepSeek/Kimi/MiniMax/
  自定义不下发任何推理参数。未配置推理偏好的旧调用（CLI 路径）请求体与历史版本一致。
- [x] “测试连接”按草稿配置发起最小请求；Key 输入留空时回退已保存 Key；连接结果仅当前会话
  可见，不持久化、不写入诊断日志明文 Key。
- [x] 旧配置迁移：已存 base URL 反查目录确定厂商，无匹配归“自定义”，不回写 settings；
  下次保存自然落盘新字段。
- [x] 96 个测试文件、959 个单测，TypeScript、lint 与 Electron fixture e2e 通过（2026-09-21）。

### 4.8 手动选篇与全链路流式（M74–M75）

- [x] 提示词协议自含：extract 的 system prompt 内嵌 kind 六枚举值、"quote 必须是段落
  text 的连续逐字片段"与 ID 来源要求；测试断言锁住 prompt 含枚举值（真实供应商曾因
  契约未送达整批校验失败）。
- [x] `WXKIT_DEBUG=1`（`npm run dev` 自动开启）将完整请求/响应/逐条校验失败对比打到
  终端 stderr；不写 main.log（零正文红线不破）。
- [x] 素材范围新增「手动选择文章」：GUI 文库勾选弹层（上限 30 与 core 护栏一致）；
  CLI `--article <id>`（可重复/逗号分隔，与时间参数互斥）；
  指名文章不做发表时间判定；文库缺失 ID 显式失败（UNKNOWN_ARTICLES），不静默丢。
- [x] 所有对模型的请求走 SSE 流式（含测试连接，首字节延迟透出）；端点不支持流时
  按 JSON 回退解析，功能不倒退。
- [x] GUI 实时输出面板：正文与思考分段展示、自动滚底、150ms 节流；切页再回来已生成
  文本不丢（store 环形尾部缓冲）；运行结束清空。
- [x] 取消 90s 总超时：长生成不设时限，用户随时可取消；仅保留空闲上限
  （连接建立后连续 180s 无字节判死，防服务端黑洞假死）。
- [x] fixture e2e 模型真发 SSE 分块，流式解析路径进端到端；98 个测试文件、990 个
  单测、TypeScript、lint 与 Electron e2e 通过（2026-09-22）。

### 4.9 手动选稿弹层改版（M77）

- [x] 弹层由「单一搜索框 + 全量平铺」改为**筛选为主、搜索为辅**：时间快捷段
  （近 7/30/90 天、今年、全部；默认近 30 天）+ 来源下拉（公众号 / 墨问作者分组，
  篇数随当前时间段变化）+ 排序（默认发布时间最新在前，可切最早在前或按来源分组）。
- [x] 关键词搜索为辅助位：只匹配标题与来源，placeholder 明说「可留空」；不搜正文
  （正文匹配命中无法预期，用户算不清漏了什么）。
- [x] 列表按日期或来源分组 + 粘性组头；渐进渲染 40 条/页，底部「加载更多（还有 N 篇）」；
  已选行高亮。
- [x] 已选跨筛选保留：底部提示「其中 N 篇不在当前条件内，已保留」；「全选命中 /
  清空命中」只作用于当前命中集，不误伤筛选外的勾选；30 篇上限触顶明确提示。
- [x] 无可解析发表时间的条目在非「全部」时间段下排除，顶部告知另有 N 篇、切到
  「全部」即可看到（与 core 的 unknown-publication-time 同一口径，不静默吞）。
- [x] 空态显示卡住的条件并给「放宽到全部时间 / 重置全部条件」；主按钮写结果
  「用这 N 篇分析」，未选时禁用。
- [x] 筛选逻辑抽为纯函数 `src/renderer/topic-manual-pick.ts`：来源等价类归并复用
  `library-view`（按身份筛选不丢无 accountId 的旧条目）、排序复用 `library-sort`
  （无时间恒置末尾）；20 条单测覆盖时间边界、来源归并、关键词、排序、分组与已选保留。

### 4.10 输入素材收敛、选题历史与选稿辅助（M78）

- [x] GUI 素材入口收敛为「输入素材」：删范围下拉与自定义日期，工具栏只剩选文章入口；
  core/CLI 时间窗口能力不变。
- [x] 历史选题：工具栏入口列出全部 run（时间倒序，含状态与候选标题）；回看以本地 state
  载入历史 result，不污染当前分析现场；删除双选项——「仅从列表移除」（localStorage 隐藏）
  与「删除记录和本地文件」（物理清除，删除弹窗按选择变形为危险态确认）。
- [x] 「看一眼」摘要：作者自写摘要（digest）无条件优先；缺失时懒加载正文 markdown 截断
  200 字兜底并注明「作者没写摘要」；两者皆无如实提示；结果按文章缓存，展开/收起不影响已选。
- [x] 「查看原文」跳文库阅读器（复用 md/html 双视图渲染），不经由自制抽屉；返回按钮
  「返回选稿」，回去后弹层自动重开、已选与筛选保留；从阅读器绕路导航不误触发重开
  （sessionStorage 标记三态：跳出未返回 / 经返回按钮 / 无）。
- [x] 阅读器返回按钮按来源回跳：下载历史、订阅（公众号与墨问面板）、文库四类入口
  跳转时携带 `state.from`，返回文案随来源（返回下载/订阅/文库），不再一律回文库。
- [x] e2e 补齐上述链路断言（摘要正文截断路径、阅读器往返、弹层重开与已选保留、
  订阅/下载入口原路返回与文案）；1029 个单测、TypeScript、lint 与 Electron fixture
  e2e 全部通过（2026-09-24）。

## 5. 版本与里程碑

- **M67**：选题基础契约、时间规则、合成评估集、旧 compose 当前入口退场。
- **M68**：正文快照、两阶段模型接口、运行时校验、统计重算、运行存储与简报。
- **M69**：OpenAI Chat Completions 兼容适配、BYOK CLI、模型通道诊断。
- **M70**：安全 Key 保存、IPC/preload、选题 GUI、反馈、简报和 Electron e2e。
- **M71**：设置页左侧分类导航、状态摘要、分组内容和统一粘性保存。
- **M72**：设置页与已确认左侧导航原型的视觉、布局和分组保真复原。
- **M73**：多厂商 AI 模型设置——厂商目录、计费模式、推理开关与五档等级、测试连接、
  旧配置迁移。
- **M74**：提示词协议自含（kind 枚举/逐字定义进 prompt）+ dev 模式终端交互追踪。
- **M75**：手动选篇（GUI 勾选/CLI --article）+ 全链路 SSE 流式 + 取消总超时改空闲上限。
- **M76**：提示词升格为契约一等公民（`prompts/contract.ts` 单一真相源 + 指令文本由常量生成 + 遍历断言）。
- **M77**：手动选稿弹层改版——筛选为主、搜索为辅（时间/来源/排序/关键词、分组列表、已选保留、渐进渲染）。
- **M78**：输入素材收敛（GUI 只留人工选篇）+ 选题历史（列举/回看/双选项删除）+ 选稿「看一眼」摘要与「查看原文」往返 + 阅读器返回按来源回跳。
- 目标版本：**v0.12.0（未发布）**。当前 `package.json` 仍为 v0.11.3，正式发版时按统一规约升级。
