# M3.5 实现计划 — 公众号批量爬取 GUI 页

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 M3 已验证的批量爬取能力补一个 GUI 页：整页登录引导 → 搜号 → 选号 → 选范围/格式 → 实时逐篇下载（可取消/重试）。

**Architecture:** GUI 接线为主，核心爬取逻辑全复用 M3。两处纯逻辑改动（`DownloadQueue` 加取消、`mp-crawl` 加逐篇上报）走 TDD；IPC 薄委派到 M3 服务；React 组件建完用 tsc + 真链路半自动验证（session 已持久在 userData，过期前免扫码）。

**Tech Stack:** TypeScript、React、antd v6、Electron IPC、vitest。

**设计依据：** `docs/superpowers/specs/2026-06-07-m3.5-batch-crawl-gui-design.md`（单页渐进 / 整页登录引导 / 实时逐篇；session 留 userData；取消=停后续保留已下；重试复用 download）。

---

## File Structure

| 文件 | 改动 | 职责 |
|---|---|---|
| `src/core/mp-types.ts` | 改 | 加 `CrawlItemStatus` / `CrawlItemEvent` |
| `src/core/download-queue.ts` | 改 | `run` 加可选 `shouldContinue`（取消） |
| `src/core/mp-crawl.ts` | 改 | `CrawlDeps` 加 `onListed`/`onItem`/`shouldContinue`，下载阶段逐篇上报 |
| `src/renderer/eta.ts` | 建 | `estimateRemaining` 纯函数 |
| `src/renderer/api.ts` | 改 | 加 `CrawlEvent` 类型 + mp 方法签名 |
| `electron/preload.ts` | 改 | 接线 mp 通道 |
| `electron/ipc.ts` | 改 | mp:login/authStatus/search/crawl(+progress/cancel) 处理器 |
| `src/renderer/layouts/MainLayout.tsx` | 改 | 加「批量」导航项 |
| `src/renderer/App.tsx` | 改 | 加 `/batch` 路由 |
| `src/renderer/components/LoginGate.tsx` | 建 | 整页登录引导 |
| `src/renderer/components/CrawlProgress.tsx` | 建 | 实时逐篇列表 + 进度 + 取消 + 重试 |
| `src/renderer/pages/BatchCrawl.tsx` | 建 | 单页编排 |
| `src/renderer/index.css` | 改 | 批量页少量样式（候选卡、范围行） |
| `tests/core/download-queue.test.ts` | 改 | 取消测试 |
| `tests/core/mp-crawl.test.ts` | 改 | 上报/取消测试 |
| `tests/renderer/eta.test.ts` | 建 | ETA 测试 |
| `tests/e2e/gui.e2e.mjs` | 改 | 批量导航 + LoginGate 渲染断言 |

---

## Task 1: DownloadQueue 取消（shouldContinue）

**Files:**
- Modify: `src/core/download-queue.ts`
- Test: `tests/core/download-queue.test.ts`

- [ ] **Step 1: 追加失败测试**

```ts
// tests/core/download-queue.test.ts —— 追加（文件顶部已 import DownloadQueue）
import { describe, it, expect } from 'vitest'
import { DownloadQueue } from '../../src/core/download-queue'

describe('DownloadQueue cancel', () => {
  it('stops before the next item when shouldContinue returns false', async () => {
    const seen: string[] = []
    const q = new DownloadQueue(async (u) => { seen.push(u); return { url: u, ok: true, id: u } })
    let calls = 0
    const summary = await q.run(['a', 'b', 'c'], () => { calls++; return calls <= 1 })
    expect(seen).toEqual(['a'])
    expect(summary.items).toHaveLength(1)
    expect(summary.total).toBe(3)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run tests/core/download-queue.test.ts -t cancel`
Expected: FAIL（`run` 还不接受第二参/不检查）。

- [ ] **Step 3: 改实现**

把 `src/core/download-queue.ts` 的 `run` 方法替换为：

```ts
  async run(urls: string[], shouldContinue?: () => boolean): Promise<DownloadSummary> {
    const items: DownloadItemResult[] = []
    const total = urls.length

    for (let i = 0; i < total; i++) {
      if (shouldContinue && !shouldContinue()) break
      const url = urls[i]
      this.onProgress({ total, completed: i, currentUrl: url, phase: 'fetch' })
      try {
        const r = await this.downloadOne(url)
        items.push(r)
        this.onProgress({ total, completed: i + 1, currentUrl: url, phase: 'save' })
      } catch (err) {
        items.push({ url, ok: false, error: { code: 'DOWNLOAD_FAILED', message: (err as Error).message } })
        this.onProgress({ total, completed: i + 1, currentUrl: url, phase: 'failed' })
      }
    }

    const succeeded = items.filter(i => i.ok && !i.skipped).length
    const skipped = items.filter(i => i.ok && i.skipped).length
    const failed = items.filter(i => !i.ok).length
    this.onProgress({ total, completed: items.length, currentUrl: '', phase: 'done' })

    return { ok: failed === 0, total, succeeded, failed, skipped, items }
  }
```

- [ ] **Step 4: 运行确认通过（含既有队列测试不回归）**

Run: `npx vitest run tests/core/download-queue.test.ts`
Expected: PASS（既有 5 + 新增 1）。

- [ ] **Step 5: 提交**

```bash
git add src/core/download-queue.ts tests/core/download-queue.test.ts
git commit -m "feat(m3.5): DownloadQueue optional shouldContinue (cancel)"
```

---

## Task 2: mp-crawl 逐篇上报 + 取消透传

**Files:**
- Modify: `src/core/mp-types.ts`
- Modify: `src/core/mp-crawl.ts`
- Test: `tests/core/mp-crawl.test.ts`

- [ ] **Step 1: 加共享类型到 `src/core/mp-types.ts`（文件末尾追加）**

```ts
export type CrawlItemStatus = 'downloading' | 'ok' | 'skipped' | 'failed'
export interface CrawlItemEvent { index: number; status: CrawlItemStatus; error?: string }
```

- [ ] **Step 2: 追加失败测试到 `tests/core/mp-crawl.test.ts`**

```ts
// tests/core/mp-crawl.test.ts —— 追加 import
import type { CrawlItemEvent } from '../../src/core/mp-types'

describe('crawlAccount reporting & cancel', () => {
  it('emits onListed once then per-item statuses in order', async () => {
    const listedCalls: string[][] = []
    const events: CrawlItemEvent[] = []
    const downloadOne = async (url: string) => {
      if (url === 'b') throw new Error('x')
      return { url, ok: true, skipped: url === 'c', id: url }
    }
    await crawlAccount('FID', { count: 3 }, {
      listFn: async () => refs(['a', 'b', 'c']),
      mpFetch: (async () => ({})) as never, token: 'T', downloadOne, sleep: noSleep,
      onListed: (r) => listedCalls.push(r.map((x) => x.url)),
      onItem: (e) => events.push(e),
    })
    expect(listedCalls).toEqual([['a', 'b', 'c']])
    expect(events).toEqual([
      { index: 0, status: 'downloading' }, { index: 0, status: 'ok' },
      { index: 1, status: 'downloading' }, { index: 1, status: 'failed', error: 'x' },
      { index: 2, status: 'downloading' }, { index: 2, status: 'skipped' },
    ])
  })

  it('stops remaining items when shouldContinue is false', async () => {
    const seen: string[] = []
    let n = 0
    const out = await crawlAccount('FID', { count: 3 }, {
      listFn: async () => refs(['a', 'b', 'c']),
      mpFetch: (async () => ({})) as never, token: 'T', sleep: noSleep,
      downloadOne: async (url) => { seen.push(url); return { url, ok: true, id: url } },
      shouldContinue: () => { n++; return n <= 1 },
    })
    expect(seen).toEqual(['a'])
    expect(out.succeeded).toBe(1)
  })
})
```

- [ ] **Step 3: 运行确认失败**

Run: `npx vitest run tests/core/mp-crawl.test.ts -t "reporting & cancel"`
Expected: FAIL（`onListed`/`onItem`/`shouldContinue` 未实现）。

- [ ] **Step 4: 改 `src/core/mp-crawl.ts`**

在 `import type` 行加入新类型，并改 `CrawlDeps` 与 `crawlAccount` 的下载阶段：

```ts
import type { MpFetch, ArticleRef, CrawlRange, CrawlSummary, CrawlItemEvent } from './mp-types'
```

`CrawlDeps` 接口追加三个可选字段：

```ts
  onListed?: (refs: ArticleRef[]) => void
  onItem?: (ev: CrawlItemEvent) => void
  shouldContinue?: () => boolean
```

把下载阶段（从 `const delayed = ...` 到 `const s = await queue.run(...)`）替换为：

```ts
  deps.onListed?.(refs)

  // 逐篇：上报「下载中→结果」，延迟在每篇前；index 经闭包计数（串行，顺序稳定）
  let index = -1
  const wrapped = async (url: string) => {
    const i = ++index
    deps.onItem?.({ index: i, status: 'downloading' })
    await sleep(randMs(2000, 5000))
    try {
      const r = await deps.downloadOne(url)
      deps.onItem?.({ index: i, status: r.skipped ? 'skipped' : 'ok' })
      return r
    } catch (e) {
      deps.onItem?.({ index: i, status: 'failed', error: (e as Error).message })
      throw e
    }
  }
  const queue = new DownloadQueue(wrapped, deps.onProgress)
  const s = await queue.run(refs.map((r) => r.url), deps.shouldContinue)
```

- [ ] **Step 5: 运行确认通过（含既有 mp-crawl 测试不回归）**

Run: `npx vitest run tests/core/mp-crawl.test.ts`
Expected: PASS（既有 3 + 新增 2）。

- [ ] **Step 6: 提交**

```bash
git add src/core/mp-types.ts src/core/mp-crawl.ts tests/core/mp-crawl.test.ts
git commit -m "feat(m3.5): mp-crawl per-item reporting + cancel passthrough"
```

---

## Task 3: ETA 纯函数

**Files:**
- Create: `src/renderer/eta.ts`
- Test: `tests/renderer/eta.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// tests/renderer/eta.test.ts
import { describe, it, expect } from 'vitest'
import { estimateRemaining } from '../../src/renderer/eta'

describe('estimateRemaining', () => {
  it('estimates from completed count and elapsed time', () => {
    // 2 篇用了 20s → 10s/篇，剩 8 篇 → 80s
    expect(estimateRemaining(2, 10, 20000)).toBe('约剩 1 分 20 秒')
  })
  it('uses seconds when under a minute', () => {
    expect(estimateRemaining(5, 10, 25000)).toBe('约剩 25 秒')
  })
  it('is blank before first completion or when done', () => {
    expect(estimateRemaining(0, 10, 5000)).toBe('')
    expect(estimateRemaining(10, 10, 5000)).toBe('')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run tests/renderer/eta.test.ts`
Expected: FAIL（`estimateRemaining` 未定义）。

- [ ] **Step 3: 写实现**

```ts
// src/renderer/eta.ts
/** 用已完成数 + 已耗时估算剩余时间，渲染成中文。未开始/已完成返回空串。 */
export function estimateRemaining(completed: number, total: number, elapsedMs: number): string {
  if (completed <= 0 || completed >= total) return ''
  const perItem = elapsedMs / completed
  const sec = Math.round((perItem * (total - completed)) / 1000)
  if (sec < 60) return `约剩 ${sec} 秒`
  return `约剩 ${Math.floor(sec / 60)} 分 ${sec % 60} 秒`
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run tests/renderer/eta.test.ts`
Expected: PASS（3 个）。

- [ ] **Step 5: 提交**

```bash
git add src/renderer/eta.ts tests/renderer/eta.test.ts
git commit -m "feat(m3.5): ETA pure function"
```

---

## Task 4: IPC + preload + api 接口面

**Files:**
- Modify: `src/renderer/api.ts`
- Modify: `electron/preload.ts`
- Modify: `electron/ipc.ts`

> 无单测（electron 绑定）。底层 `searchAccount`/`crawlAccount`/AUTH 行为已由 M3 的 CLI 契约测试覆盖。本任务靠 `tsc` 把关类型，真链路在 Task 7 验证。

- [ ] **Step 1: `src/renderer/api.ts` 增类型与方法**

顶部 import 增补：

```ts
import type { MpAccount, CrawlSummary, CrawlItemStatus } from '../core/mp-types'
```

在 `declare global` 之前加 `CrawlEvent` 与范围类型：

```ts
export interface CrawlRangeInput { count?: number; from?: string; to?: string }
export type CrawlEvent =
  | { kind: 'listed'; items: { title: string; url: string }[] }
  | { kind: 'item'; index: number; status: CrawlItemStatus; error?: string }
  | { kind: 'done'; summary: CrawlSummary }
```

在 `WxApi` 接口里追加：

```ts
  mpAuthStatus(): Promise<{ valid: boolean }>
  mpLogin(): Promise<{ ok: boolean; error?: string }>
  mpSearch(name: string): Promise<{ ok: boolean; list?: MpAccount[]; error?: { code: string; message: string } }>
  mpCrawl(fakeid: string, range: CrawlRangeInput, formats: DownloadFormat[]): Promise<CrawlSummary>
  onCrawlProgress(cb: (e: CrawlEvent) => void): () => void
  mpCancelCrawl(): void
```

- [ ] **Step 2: `electron/preload.ts` 增接线（在 `reveal` 行后）**

```ts
  mpAuthStatus: () => ipcRenderer.invoke('mp:authStatus'),
  mpLogin: () => ipcRenderer.invoke('mp:login'),
  mpSearch: (name) => ipcRenderer.invoke('mp:search', name),
  mpCrawl: (fakeid, range, formats) => ipcRenderer.invoke('mp:crawl', { fakeid, range, formats }),
  onCrawlProgress: (cb) => {
    const listener = (_e: unknown, ev: Parameters<typeof cb>[0]) => cb(ev)
    ipcRenderer.on('mp:crawl:progress', listener)
    return () => { ipcRenderer.removeListener('mp:crawl:progress', listener) }
  },
  mpCancelCrawl: () => ipcRenderer.send('mp:crawl:cancel'),
```

- [ ] **Step 3: `electron/ipc.ts` 增 import + 处理器**

顶部 import 增补：

```ts
import { login, getSession } from './services/mp-auth'
import { makeMpFetch } from './services/mp-fetch'
import { searchAccount } from '../src/core/mp-client'
import { crawlAccount } from '../src/core/mp-crawl'
import { downloadArticle } from '../src/core/download-article'
import { MpAuthExpired } from '../src/core/mp-errors'
import type { CrawlRange } from '../src/core/mp-types'
```

在 `registerIpc` 内、`download` 处理器之后追加：

```ts
  ipcMain.handle('mp:login', async () => {
    try { await login(); return { ok: true } }
    catch (e) { return { ok: false, error: (e as Error).message } }
  })

  ipcMain.handle('mp:authStatus', async () => {
    const session = getSession()
    if (!session) return { valid: false }
    try { await searchAccount(makeMpFetch(session), session.token, '腾讯'); return { valid: true } }
    catch (e) { if (e instanceof MpAuthExpired) return { valid: false }; throw e }
  })

  ipcMain.handle('mp:search', async (_e, name: string) => {
    const session = getSession()
    if (!session) return { ok: false, error: { code: 'AUTH_REQUIRED', message: '请先登录公众号后台' } }
    try { return { ok: true, list: await searchAccount(makeMpFetch(session), session.token, name) } }
    catch (e) {
      const code = e instanceof MpAuthExpired ? 'AUTH_REQUIRED' : 'MP_API_ERROR'
      return { ok: false, error: { code, message: (e as Error).message } }
    }
  })

  let cancelRequested = false
  ipcMain.on('mp:crawl:cancel', () => { cancelRequested = true })
  ipcMain.handle('mp:crawl', async (event, { fakeid, range, formats }: { fakeid: string; range: CrawlRange; formats: DownloadFormat[] }) => {
    cancelRequested = false
    const session = getSession()
    if (!session) throw new Error('AUTH_REQUIRED')
    const { libraryRoot } = await settings.get()
    const library = new Library(libraryRoot)
    const ddeps = { fetchHtml, fetchBinary, BrowserWindowCtor: BrowserWindow, now: () => new Date().toISOString(), library, libraryRoot }
    const send = (ev: unknown) => { if (!event.sender.isDestroyed()) event.sender.send('mp:crawl:progress', ev) }
    const summary = await crawlAccount(fakeid, range, {
      mpFetch: makeMpFetch(session), token: session.token,
      downloadOne: (url) => downloadArticle(url, formats, ddeps),
      onListed: (refs) => send({ kind: 'listed', items: refs.map((r) => ({ title: r.title, url: r.url })) }),
      onItem: (ev) => send({ kind: 'item', ...ev }),
      shouldContinue: () => !cancelRequested,
    })
    send({ kind: 'done', summary })
    return summary
  })
```

- [ ] **Step 4: 类型检查**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出。

- [ ] **Step 5: 提交**

```bash
git add src/renderer/api.ts electron/preload.ts electron/ipc.ts
git commit -m "feat(m3.5): IPC/preload/api surface for login/search/crawl"
```

---

## Task 5: 导航 + 路由 + LoginGate + CrawlProgress 组件 + 样式

**Files:**
- Modify: `src/renderer/layouts/MainLayout.tsx`
- Modify: `src/renderer/App.tsx`
- Create: `src/renderer/components/LoginGate.tsx`
- Create: `src/renderer/components/CrawlProgress.tsx`
- Modify: `src/renderer/index.css`

- [ ] **Step 1: 导航加「批量」项** — `MainLayout.tsx` 的 `NAV` 数组改为：

```ts
const NAV = [
  { to: '/', label: '下载', end: true },
  { to: '/batch', label: '批量', end: false },
  { to: '/library', label: '书架', end: false },
  { to: '/settings', label: '设置', end: false },
]
```

- [ ] **Step 2: 路由加 `/batch`** — `App.tsx`：import 并加 Route。

```tsx
import BatchCrawl from './pages/BatchCrawl'
```

在 `<Route index .../>` 之后加：

```tsx
        <Route path="batch" element={<BatchCrawl />} />
```

- [ ] **Step 3: 写 `src/renderer/components/LoginGate.tsx`**

```tsx
import { useState } from 'react'
import { api } from '../api'

/** 整页登录引导：未登录时本页只显示这一屏。 */
export default function LoginGate({ onLoggedIn }: { onLoggedIn: () => void }) {
  const [busy, setBusy] = useState(false)
  const [err, setErr] = useState('')
  const doLogin = async () => {
    setBusy(true); setErr('')
    const r = await api.mpLogin()
    setBusy(false)
    if (r.ok) onLoggedIn()
    else setErr(r.error === 'CANCELLED' ? '已取消登录' : '登录失败：' + (r.error ?? ''))
  }
  return (
    <div className="empty-state" data-testid="login-gate">
      <div className="es-mark">▣</div>
      <div className="es-title">先登录公众号后台</div>
      <div style={{ maxWidth: 360 }}>批量爬取需要用你的公众号管理员身份扫码登录 mp.weixin.qq.com。登录态保存在本地，过期前无需重复扫码。</div>
      <button className="cta" disabled={busy} onClick={doLogin} data-testid="login-scan">
        {busy ? '请在弹出窗口扫码…' : '扫码登录'}
      </button>
      {err && <div className="faint" style={{ color: 'var(--cinnabar)' }}>{err}</div>}
    </div>
  )
}
```

- [ ] **Step 4: 写 `src/renderer/components/CrawlProgress.tsx`**

```tsx
export interface CrawlRow { title: string; url: string; status: 'waiting' | 'downloading' | 'ok' | 'skipped' | 'failed'; error?: string }

const ICON: Record<CrawlRow['status'], string> = { waiting: '·', downloading: '⟳', ok: '✓', skipped: '⊘', failed: '✗' }
const BADGE: Record<string, [string, string]> = { ok: ['badge-ok', '成功'], skipped: ['badge-skip', '已存在'], failed: ['badge-fail', '失败'] }

interface Props {
  account: string
  rows: CrawlRow[]
  eta: string
  running: boolean
  onCancel: () => void
  onRetry: (index: number) => void
}

export default function CrawlProgress({ account, rows, eta, running, onCancel, onRetry }: Props) {
  const done = rows.filter((r) => r.status === 'ok' || r.status === 'skipped' || r.status === 'failed').length
  const pct = rows.length ? Math.round((done / rows.length) * 100) : 0
  return (
    <div className="surface fade-in" style={{ marginTop: 24, padding: '18px 20px' }} data-testid="crawl-progress">
      <div className="progress-head">
        <span className="progress-phase">{running ? `正在爬取 · ${account}` : `已完成 · ${account}`}</span>
        {running
          ? <button className="card-btn danger" style={{ flex: 'none', border: '1px solid var(--line-strong)', padding: '2px 14px' }} data-testid="crawl-cancel" onClick={onCancel}>取消</button>
          : <span className="progress-count">{done}/{rows.length}</span>}
      </div>
      <div className="progress-track"><div className="progress-fill" style={{ width: `${pct}%` }} /></div>
      <div className="progress-current">{done}/{rows.length}{eta ? ' · ' + eta : ''}</div>
      <div className="result-list" style={{ marginTop: 10 }}>
        {rows.map((r, i) => (
          <div className="result-row" key={i}>
            <span style={{ width: 16, textAlign: 'center', color: r.status === 'downloading' ? 'var(--cinnabar)' : 'var(--ink-faint)' }}>{ICON[r.status]}</span>
            <span className="result-url" style={{ color: r.status === 'downloading' ? 'var(--ink)' : undefined }}>{r.title}</span>
            {r.status === 'downloading' && <span className="faint">下载中…</span>}
            {BADGE[r.status] && <span className={`badge ${BADGE[r.status][0]}`} title={r.error}>{BADGE[r.status][1]}</span>}
            {r.status === 'failed' && <button className="card-btn" style={{ flex: 'none' }} onClick={() => onRetry(i)}>重试</button>}
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 5: `src/renderer/index.css` 末尾追加批量页样式**

```css
/* ---------- 批量页 ---------- */
.cta {
  border: none;
  background: var(--cinnabar);
  color: #fff;
  font-family: inherit;
  font-size: 14px;
  font-weight: 600;
  padding: 9px 24px;
  border-radius: 8px;
  cursor: pointer;
  margin-top: 8px;
  transition: background 0.16s ease;
}
.cta:hover { background: var(--cinnabar-soft); }
.cta:disabled { opacity: 0.6; cursor: default; }
.candidate {
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding: 12px 14px;
  border: 1px solid var(--line-strong);
  border-radius: 8px;
  background: var(--paper-raised);
  cursor: pointer;
  transition: border-color 0.16s ease, background 0.16s ease;
}
.candidate:hover { border-color: var(--cinnabar); background: var(--cinnabar-wash); }
.candidate .c-name { font-weight: 600; font-size: 14px; }
.candidate .c-sig { font-size: 12px; color: var(--ink-soft); }
.range-row { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; }
```

- [ ] **Step 6: 类型检查 + 提交**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出（BatchCrawl 尚未建，App.tsx import 会报错——故本步骤把 Step 2 的 import 暂注释，或先跳过 tsc，待 Task 6 建好 BatchCrawl 后统一验。**改为：本任务先不验 tsc，Task 6 末尾统一验。**）

```bash
git add src/renderer/layouts/MainLayout.tsx src/renderer/App.tsx src/renderer/components/LoginGate.tsx src/renderer/components/CrawlProgress.tsx src/renderer/index.css
git commit -m "feat(m3.5): nav/route + LoginGate + CrawlProgress + batch styles"
```

> 注：App.tsx 引用了下一任务才建的 `BatchCrawl`，故 Task 5 暂不跑 tsc；Task 6 建好页面后统一类型检查。

---

## Task 6: BatchCrawl 单页编排

**Files:**
- Create: `src/renderer/pages/BatchCrawl.tsx`

- [ ] **Step 1: 写 `src/renderer/pages/BatchCrawl.tsx`**

```tsx
import { useEffect, useRef, useState } from 'react'
import { Input, Segmented, InputNumber, DatePicker, Spin, message } from 'antd'
import type { Dayjs } from 'dayjs'
import { api } from '../api'
import type { CrawlEvent, CrawlRangeInput } from '../api'
import LoginGate from '../components/LoginGate'
import CrawlProgress, { type CrawlRow } from '../components/CrawlProgress'
import FormatPicker from '../components/FormatPicker'
import { estimateRemaining } from '../eta'
import type { DownloadFormat } from '../../core/types'
import type { MpAccount } from '../../core/mp-types'

export default function BatchCrawl() {
  const [authValid, setAuthValid] = useState<boolean | null>(null)
  const [name, setName] = useState('')
  const [searching, setSearching] = useState(false)
  const [accounts, setAccounts] = useState<MpAccount[] | null>(null)
  const [selected, setSelected] = useState<MpAccount | null>(null)
  const [mode, setMode] = useState<'count' | 'date'>('count')
  const [count, setCount] = useState(10)
  const [dates, setDates] = useState<[Dayjs, Dayjs] | null>(null)
  const [formats, setFormats] = useState<DownloadFormat[]>(['md', 'html', 'meta'])
  const [running, setRunning] = useState(false)
  const [rows, setRows] = useState<CrawlRow[]>([])
  const [eta, setEta] = useState('')
  const startRef = useRef(0)

  useEffect(() => {
    api.getSettings().then((s) => setFormats(s.defaultFormats)).catch(() => {})
    api.mpAuthStatus().then((r) => setAuthValid(r.valid)).catch(() => setAuthValid(false))
  }, [])

  useEffect(() => {
    const off = api.onCrawlProgress((ev: CrawlEvent) => {
      if (ev.kind === 'listed') {
        startRef.current = Date.now()
        setRows(ev.items.map((it) => ({ title: it.title, url: it.url, status: 'waiting' })))
      } else if (ev.kind === 'item') {
        setRows((prev) => {
          const next = prev.slice()
          if (next[ev.index]) next[ev.index] = { ...next[ev.index], status: ev.status, error: ev.error }
          const done = next.filter((r) => r.status === 'ok' || r.status === 'skipped' || r.status === 'failed').length
          setEta(estimateRemaining(done, next.length, Date.now() - startRef.current))
          return next
        })
      } else if (ev.kind === 'done') {
        setRunning(false); setEta('')
      }
    })
    return off
  }, [])

  const search = async () => {
    if (!name.trim()) return
    setSearching(true); setAccounts(null); setSelected(null)
    const r = await api.mpSearch(name.trim())
    setSearching(false)
    if (!r.ok) {
      if (r.error?.code === 'AUTH_REQUIRED') setAuthValid(false)
      else message.error('搜索失败：' + (r.error?.message ?? ''))
      return
    }
    setAccounts(r.list ?? [])
  }

  const start = async () => {
    if (!selected) return
    const range: CrawlRangeInput = mode === 'count'
      ? { count }
      : dates ? { from: dates[0].format('YYYY-MM-DD'), to: dates[1].format('YYYY-MM-DD') } : {}
    if (mode === 'date' && !dates) { message.warning('请选择日期范围'); return }
    if (!formats.length) { message.warning('请至少选择一种格式'); return }
    setRunning(true); setRows([]); setEta('')
    try {
      const summary = await api.mpCrawl(selected.fakeid, range, formats)
      message.success(`完成 · 成功 ${summary.succeeded}，跳过 ${summary.skipped}，失败 ${summary.failed}`)
    } catch (e) {
      const msg = (e as Error).message
      if (msg.includes('AUTH_REQUIRED')) { setAuthValid(false); setRunning(false) }
      else { message.error('爬取出错：' + msg); setRunning(false) }
    }
  }

  const retry = async (i: number) => {
    const row = rows[i]
    setRows((prev) => prev.map((r, j) => (j === i ? { ...r, status: 'downloading' } : r)))
    const s = await api.download([row.url], formats)
    const r = s.items[0]
    setRows((prev) => prev.map((x, j) => (j === i
      ? { ...x, status: r.ok ? (r.skipped ? 'skipped' : 'ok') : 'failed', error: r.error?.message }
      : x)))
  }

  if (authValid === null) return <div className="page" style={{ textAlign: 'center', paddingTop: 80 }}><Spin /></div>
  if (!authValid) return <div className="page"><div className="page-narrow"><LoginGate onLoggedIn={() => setAuthValid(true)} /></div></div>

  return (
    <div className="page">
      <div className="page-narrow fade-in">
        <div className="page-head">
          <div className="eyebrow">Batch</div>
          <h1 className="page-title">批量爬取</h1>
          <p className="page-sub">搜索公众号，按数量或日期范围批量下载它的文章。</p>
        </div>

        <Input.Search placeholder="输入公众号名称" enterButton="搜索" value={name}
          onChange={(e) => setName(e.target.value)} onSearch={search} loading={searching}
          disabled={running} style={{ maxWidth: 420 }} data-testid="account-search" />

        {accounts && !selected && (
          <div className="fade-in" style={{ marginTop: 16, display: 'grid', gap: 8, maxWidth: 520 }}>
            {accounts.length === 0 && <div className="faint">没找到这个公众号，换个名字试试。</div>}
            {accounts.map((a) => (
              <div key={a.fakeid} className="candidate" data-testid="candidate" onClick={() => { setSelected(a); setAccounts(null) }}>
                <span className="c-name">{a.nickname}</span>
                {a.signature && <span className="c-sig">{a.signature}</span>}
              </div>
            ))}
          </div>
        )}

        {selected && !running && rows.length === 0 && (
          <div className="fade-in" style={{ marginTop: 20 }}>
            <div style={{ marginBottom: 12 }}>已选：<b className="font-serif">{selected.nickname}</b>　<a onClick={() => setSelected(null)} style={{ color: 'var(--cinnabar)', cursor: 'pointer' }}>换一个</a></div>
            <div className="range-row" style={{ marginBottom: 14 }}>
              <Segmented value={mode} onChange={(v) => setMode(v as 'count' | 'date')}
                options={[{ label: '最近 N 篇', value: 'count' }, { label: '日期范围', value: 'date' }]} />
              {mode === 'count'
                ? <InputNumber min={1} max={200} value={count} onChange={(v) => setCount(v ?? 1)} addonAfter="篇" />
                : <DatePicker.RangePicker value={dates ?? undefined} onChange={(v) => setDates(v as [Dayjs, Dayjs] | null)} />}
            </div>
            <div style={{ margin: '6px 0 10px', fontWeight: 600 }}>保存为</div>
            <FormatPicker value={formats} onChange={setFormats} />
            <button className="cta" style={{ marginTop: 20 }} onClick={start} data-testid="start-crawl">开始爬取</button>
          </div>
        )}

        {(running || rows.length > 0) && (
          <CrawlProgress account={selected?.nickname ?? ''} rows={rows} eta={eta} running={running}
            onCancel={() => api.mpCancelCrawl()} onRetry={retry} />
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 全量类型检查**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出。若报 `dayjs` 类型缺失，确认 antd v6 已带 dayjs（`npm ls dayjs` 应有）；RangePicker 的 onChange 值用 `as [Dayjs, Dayjs] | null` 已断言。

- [ ] **Step 3: 全量单测 + 构建冒烟**

Run: `npx vitest run && npx vite build`
Expected: vitest 全绿；vite build 成功。

- [ ] **Step 4: 提交**

```bash
git add src/renderer/pages/BatchCrawl.tsx
git commit -m "feat(m3.5): BatchCrawl page (search/pick/range/format/live progress)"
```

---

## Task 7: 真链路半自动验证 + e2e + 收尾

**Files:**
- Modify: `tests/e2e/gui.e2e.mjs`
- Modify: `ROADMAP.md`、`docs/devlog/wx-kit-vibe-coding.md`

- [ ] **Step 1: e2e 加「批量导航 + 登录引导」断言**

在 `tests/e2e/gui.e2e.mjs` 的 settings 断言之后、`screenshot` 之前追加（e2e 环境无 mp-session，故批量页应显示 LoginGate）：

```js
    // --- batch page shows login gate when no session ---
    await win.click('[data-testid="nav-批量"]')
    await win.waitForSelector('[data-testid="login-gate"]', { timeout: 10000 })
    assert(true, 'batch page shows login gate without a session')
```

- [ ] **Step 2: 跑 e2e**

Run: `npm run test:e2e`
Expected: 全部断言 PASS（含新增的批量登录引导）。

- [ ] **Step 3: 真链路半自动验证（用已持久化 session；过期才喊安哥扫码）**

先确认 session 有效：

Run: `npx electron . auth-status`
Expected: `{"ok":true,"valid":true}`。若 `valid:false` → `npx electron . login`（安哥扫一次码）。

启动 GUI 手动走一遍：

Run: `npm run dev`
人工核对：批量页 → 搜「腾讯」→ 候选列表出现 → 选「腾讯」→ 选「最近 2 篇」+ 格式 → 开始 → 逐篇行实时翻 ✓/⊘ → 点一次取消（中途）确认停止且已下保留 → 对失败/未下项点重试。

**GATE：** 若实时列表不更新、取消无效、或 IPC 报错，回对应 Task 修正。

- [ ] **Step 4: 更新状态文档**

- `ROADMAP.md`：M3.5 行状态改 `✅ 已合入 main`；当前状态补一句「批量爬取已有 GUI（搜号→选范围→实时逐篇→取消/重试）」。
- `docs/devlog/wx-kit-vibe-coding.md`：按工作流第 6 条增补 M3.5 一节（可视化伴侣定三大交互、取消=停后续保留已下、session 留 userData 换来免扫码自动化、复用 download 做重试）。

- [ ] **Step 5: 提交**

```bash
git add tests/e2e/gui.e2e.mjs ROADMAP.md docs/devlog/wx-kit-vibe-coding.md
git commit -m "test+docs(m3.5): batch e2e login-gate assert; mark M3.5 done"
```

---

## 验收对照（self-review 已核）

- 整页登录引导 → Task 5 LoginGate + Task 6 authValid 分支；扫码调 `mpLogin` → Task 4。
- 单页渐进（搜→选→范围/格式→进度）→ Task 6 条件渲染。
- 实时逐篇（listed 铺行 + item 翻状态）→ Task 2（上报）+ Task 4（IPC 转 CrawlEvent）+ Task 6（订阅渲染）。
- 预计剩余 → Task 3 + Task 6。
- 取消=停后续、已下保留 → Task 1（队列）+ Task 2（透传）+ Task 6（按钮）。
- 单篇重试复用 download → Task 6 `retry`。
- 错误：AUTH_REQUIRED 弹回 LoginGate（Task 6）、搜号空引导（Task 6）、失败行内重试（Task 5/6）。
- session 留 userData、过期才扫码 → 无存储改动；验证半自动（Task 7）。
- 非目标（暂停/多号并行）不在计划内。
```
