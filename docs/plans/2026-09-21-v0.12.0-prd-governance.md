# v0.12.0 PRD 与文档分层修正实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. 本任务只修正文档契约和版本归属，不修改业务代码或提前发版。

**Goal:** 为 M67–M70 补齐 v0.12.0 产品验收契约，并消除 AGENTS、设计文档、实现计划之间的需求重复和第二真相源。

**Architecture:** `docs/PRD-v0.12.0.md` 成为当前未发布选题能力的唯一产品范围与逐条验收契约；设计文档保留方案依据，`docs/plans/` 保留实施步骤，AGENTS 只保留跨版本工程与安全不变量，ROADMAP 只维护版本/里程碑状态和索引。

**Tech Stack:** Markdown、Git、现有 Skill validator；不改 TypeScript/Electron 实现。

**Spec:** `docs/superpowers/specs/2026-09-20-topic-decisions-design.md`（设计依据）；本计划完成后以 `docs/PRD-v0.12.0.md` 为产品验收契约。

## Global Constraints

- v0.12.0 是 M67–M70 的目标版本，当前仍未发布。
- `package.json` / `package-lock.json` 保持 v0.11.3；只有正式发版流程才 bump。
- 不改写 M67–M70 已实现行为，不把 fixture/e2e 说成真实供应商或语义质量已验。
- 历史 PRD、计划、设计和 devlog 保留，不抹去此前文档分层错误的事实。
- AGENTS 不复制页面布局、默认范围、当前非目标等版本级需求，只保存长期防回退约束并指向 PRD。

---

## Task 1：建立 v0.12.0 PRD

**Files:**
- Create: `docs/PRD-v0.12.0.md`

- [x] 写明用户问题、目标用户、首版闭环、范围与非目标。
- [x] 把 M67–M70 的时间、证据、GUI、CLI、BYOK、反馈、简报和错误状态整理成产品需求，不复制实现细节。
- [x] 第 4 节给出逐条验收：已由自动化证实的项目勾选；真实自有文库、真实用户指定供应商、语义质量和正式发版保持未勾选。
- [x] 明确完整正文和敏感信息的边界：模型请求需要正文；result 可保留通过校验的必要摘录，但不得复制整篇正文；stdout/trace/诊断日志不得泄漏正文或密钥。

## Task 2：收缩 AGENTS 为长期不变量

**Files:**
- Modify: `AGENTS.md`

- [x] 将“选题能力边界”改为“选题模块长期不变量”，删除页面布局、默认 24 小时、本期非目标和当前闭环等版本需求。
- [x] 保留单一 core、模型/确定性计算分离、证据与统计真实性、外部服务隐私和评估数据卫生。
- [x] 文档索引增加 v0.12.0 PRD，并声明当前产品范围与验收只看 PRD。

## Task 3：统一版本归属与文档引用

**Files:**
- Modify: `ROADMAP.md`
- Modify: `docs/superpowers/specs/2026-09-20-topic-decisions-design.md`
- Modify: `docs/plans/2026-09-20-m67-topic-foundation.md`
- Modify: `docs/plans/2026-09-20-m68-verifiable-topic-analysis.md`
- Modify: `docs/plans/2026-09-20-m69-byok-topics-cli.md`
- Modify: `docs/plans/2026-09-20-m70-topics-gui.md`
- Modify: `docs/devlog/wx-kit-vibe-coding.md`

- [x] ROADMAP 将 M67–M70 版本列统一为“v0.12.0（未发布）”，PRD 索引加入 v0.12.0。
- [x] 设计文档明确自身是方案依据，不是验收契约；四份计划补目标版本与 PRD 引用。
- [x] devlog 新增一次文档治理复盘，记录遗漏原因、影响与修正后的分层规则。

## Task 4：同步 agent Skill 的版本口径

**Files:**
- Modify: `agent/wx-kit-skill/SKILL.md`
- Modify: `agent/wx-kit-skill/references/commands.md`
- Modify: `agent/wx-kit-skill/references/recipes.md`
- Modify if matched: `agent/README.md`

- [x] 将“当前 main / M70 未发布”统一为“v0.12.0 当前 main 未发布”，继续明确 v0.11.3 安装包没有选题能力。
- [x] 不改变命令参数、数据契约或安全说明。

## Task 5：验证与收尾

- [x] 检索 M67–M70 的版本归属、PRD 索引和 AGENTS 中残留的版本级需求。
- [x] 检查所有新增/修改 Markdown 链接可解析，`git diff --check` 通过。
- [x] 运行 `quick_validate.py agent/wx-kit-skill`。
- [x] 文档改动不重跑 Electron e2e，但仍按项目规约运行 `npm test`、`npm run lint`、`npx tsc --noEmit -p tsconfig.json`；检查 `package.json` 仍为 0.11.3，工作树仅含预期文档。
- [x] commit -F，合并 main，删除 feature 分支，不 push。
