# M25 · 体验杂项:排序默认+记忆 / 日志入口 / CLI 帮助（v0.6.0 R1–R3）

> 需求/验收见 `docs/PRD-v0.6.0.md` R1–R3。分支 `feat/m25-ux-misc`。

## 1. R1 · 排序默认值 + 跨会话记忆

- `electron/services/settings.ts`:`AppSettings` 加 `librarySort: { key: SortKey; dir: SortDir }`,默认 `{ key: 'publish', dir: 'desc' }`(SortKey/SortDir 类型从 renderer 侧上移或在 settings 内联定义,避免主进程 import renderer)。
- `src/renderer/pages/Library.tsx`:`sortKey`/`sortDir` 初始值改从 settings 读(load() 时 set);`setSortKey`/`setSortDir`/`onHeaderSort` 的变更点统一走一个 `changeSort(key, dir)`,内含 fire-and-forget `saveSettings({ librarySort })`(与列宽、展开态同模式)。
- 单测:settings 默认值(settings.test.ts 期望对象加字段);sortArticles 空 publishTime 恒末尾已有测试,不动。
- e2e:适配——文库初始断言从「下载时间」语境改「发布时间降序」;fixture 文章有 publish 时间,断言最新发表在最前;切排序 → 导航离开回来仍保持。

## 2. R2 · 检查日志入口

- `src/renderer/pages/Settings.tsx` 订阅区块末尾加一行:说明文案「完整检查历史(含每次失败原因)」+ Button「打开检查日志」(`data-testid="set-open-checklog"`),onClick `api.subscriptionsOpenLog()`(ipc 已确保文件存在,零新后端)。
- `src/renderer/pages/Subscriptions.tsx`:原小字 `<a>打开日志文件</a>` 文案统一为「打开检查日志」,样式升级为带 📄 图标的链接(保持行内,不新增按钮层级)。
- e2e:设置页断言入口存在;订阅页既有 open-log 断言文案同步改。

## 3. R3 · CLI 帮助完善

`src/cli/index.ts`:

- 顶层 description 扩写:
  `微信百宝箱 CLI — 与 GUI 同一二进制:无参启动图形界面,带子命令进入 CLI。\nstdout 输出纯 JSON(数据),stderr 输出进度,退出码 0=成功 1=业务失败 2=用法/鉴权错误。`
- 命令组 description 透出子命令:`library` → `文章库(子命令:list / search / remove / rebuild / export)`;`subscription` → `公众号订阅(子命令:list / check-now)`;`settings` → `读写应用设置(子命令:get / set)`。
- `program.addHelpText('after', ...)` 追加「常用示例」:download(md,pdf)、crawl --count + --include、library list、library export、settings get libraryRoot;末行注明库默认位置 `~/Documents/wx-kit`(可在设置改)。
- 单测(`tests/electron/cli-dispatch.test.ts` 或新增 cli-help 测试):帮助文本含「无参启动图形界面」「退出码」「子命令:list」「示例」关键串;`-h` 退出码 0。
- 真机:`npx electron . -h` 与打包态 `-h` 目检一屏可读。

## 4. 验证与收尾

`npm test` / lint / tsc / `npm run test:e2e`;GUI 真机(真实文库):默认发布时间降序截图 + 切排序重启保持;合 main 删分支。M25 不单独发版,随 v0.6.0 最终发版走。
