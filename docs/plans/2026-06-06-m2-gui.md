# wx-kit M2：GUI（URL 下载 + 文章库 + 内置阅读器）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 wx-kit 加上桌面 GUI：URL 多链接下载（带实时进度）、文章库（搜索/删除/在文件夹显示）、内置阅读器（md/html），以及设置页（库根目录、默认格式）。

**Architecture:** 渲染层（React + Antd + Tailwind）绝不直接 import core；通过 preload 的 `contextBridge` 暴露的 `window.api` 走 IPC 调主进程，主进程调用 M1 已有的 core（download-queue / library / exporter）。本地文章图片通过自定义 `wxfile://` 协议读取，保证相对路径图片可显示且不放开 webSecurity。

**Tech Stack:** React 18 + TypeScript + Vite（已有）；新增 Antd 6 + Tailwind 3 + react-router-dom 7 + react-markdown + remark-gfm。测试 vitest（已有）。

**Scope:** 仅 PRD 的 M2（F1 的 GUI 形态 + F3 文章库与阅读器 + 设置壳）。**不含** 公众号批量爬取/登录（M3）与打包（M4）。CLI（M1）保持不变。导航只放「URL 下载 / 文章库 / 设置」。

> 前置：M1 已合入 main（core 层 + CLI 完整，50 单测通过）。本计划假定执行者熟悉 React/TS 但不了解本项目。每步给确切路径、完整代码、运行命令与期望。可测接缝走 TDD；React 页面用启动应用手动验证（可配合 Playwright/webapp-testing）。

---

## 文件结构（先锁定边界）

```
electron/
├─ main.ts                  # 修改：注册 wxfile 协议 + registerIpc + GUI 窗口（CLI 分支不变）
├─ preload.ts               # 修改：contextBridge 暴露 window.api，导出 WxApi 类型
├─ ipc.ts                   # 新增：registerIpc(getWin) 注册所有 ipcMain 处理器
├─ protocol.ts              # 新增：wxfile:// 协议（resolveWxfilePath 纯函数 + 注册）
└─ services/
   └─ settings.ts           # 新增：SettingsService（userData 持久化）
src/core/
└─ read-article.ts          # 新增：readArticleContent(dir, kind) 读 md(去 frontmatter)/html
src/renderer/
├─ main.tsx                 # 修改：挂载 Router + antd ConfigProvider + 引入 tailwind css
├─ App.tsx                  # 修改：路由表
├─ index.css                # 新增：tailwind 指令
├─ api.ts                   # 新增：window.api 的类型化封装 + 共享 DTO 类型
├─ layouts/MainLayout.tsx   # 新增：Sider 菜单 + 内容区
├─ pages/UrlDownload.tsx    # 新增：多行 URL + 格式勾选 + 进度
├─ pages/Library.tsx        # 新增：搜索 + 列表 + 操作（阅读/显示/删除）
├─ pages/Reader.tsx         # 新增：md(react-markdown)/html(iframe) 阅读
└─ pages/Settings.tsx       # 新增：库根目录 + 默认格式
tailwind.config.js          # 新增
postcss.config.js           # 新增
tests/
├─ core/read-article.test.ts          # TDD
├─ electron/settings.test.ts          # TDD
└─ electron/protocol-resolve.test.ts  # TDD（路径越界防护）
```

边界原则：IPC 处理器是「薄」委派层；真正的逻辑（settings 读写、读文章正文、协议路径解析）抽成可单测的纯/半纯函数。React 页面只调 `window.api`。

---

### Task 1: UI 依赖 + Tailwind/Antd + 应用壳与路由

**Files:**
- Modify: `package.json`（加依赖）, `src/renderer/main.tsx`, `src/renderer/App.tsx`
- Create: `tailwind.config.js`, `postcss.config.js`, `src/renderer/index.css`, `src/renderer/layouts/MainLayout.tsx`, 占位页 `src/renderer/pages/{UrlDownload,Library,Settings}.tsx`

- [ ] **Step 1: 安装依赖**

Run:
```bash
cd /Users/chenzhian/workspace/ai/wx-kit
npm install antd@^6 react-router-dom@^7 react-markdown@^9 remark-gfm@^4
npm install -D tailwindcss@^3 postcss@^8 autoprefixer@^10
```
Expected: 安装成功，无 ERR。若 antd@6 的具体版本号解析失败，取最新的 6.x。

- [ ] **Step 2: 创建 tailwind.config.js**（禁用 preflight 避免重置 Antd 样式）

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  corePlugins: { preflight: false },
  theme: { extend: {} },
  plugins: [],
}
```

- [ ] **Step 3: 创建 postcss.config.js**

```js
module.exports = { plugins: { tailwindcss: {}, autoprefixer: {} } }
```

- [ ] **Step 4: 创建 src/renderer/index.css**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

html, body, #root { height: 100%; margin: 0; }
```

- [ ] **Step 5: 创建三个占位页**（内容先放标题，后续任务填充）

`src/renderer/pages/UrlDownload.tsx`:
```tsx
export default function UrlDownload() {
  return <div className="p-6"><h2>URL 下载</h2></div>
}
```
`src/renderer/pages/Library.tsx`:
```tsx
export default function Library() {
  return <div className="p-6"><h2>文章库</h2></div>
}
```
`src/renderer/pages/Settings.tsx`:
```tsx
export default function Settings() {
  return <div className="p-6"><h2>设置</h2></div>
}
```

- [ ] **Step 6: 创建 MainLayout.tsx**

```tsx
import { Layout, Menu } from 'antd'
import { Link, Outlet, useLocation } from 'react-router-dom'
import { DownloadOutlined, BookOutlined, SettingOutlined } from '@ant-design/icons'

const items = [
  { key: '/', icon: <DownloadOutlined />, label: <Link to="/">URL 下载</Link> },
  { key: '/library', icon: <BookOutlined />, label: <Link to="/library">文章库</Link> },
  { key: '/settings', icon: <SettingOutlined />, label: <Link to="/settings">设置</Link> },
]

export default function MainLayout() {
  const { pathname } = useLocation()
  const selected = pathname.startsWith('/library') ? '/library' : pathname.startsWith('/settings') ? '/settings' : '/'
  return (
    <Layout style={{ height: '100%' }}>
      <Layout.Sider theme="light" width={200}>
        <div style={{ padding: 16, fontWeight: 600 }}>wx-kit · 微信百宝箱</div>
        <Menu mode="inline" selectedKeys={[selected]} items={items} />
      </Layout.Sider>
      <Layout.Content style={{ overflow: 'auto', background: '#fff' }}>
        <Outlet />
      </Layout.Content>
    </Layout>
  )
}
```
> 需要 `@ant-design/icons`；若未随 antd 安装，`npm install @ant-design/icons@^6`。

- [ ] **Step 7: 改 App.tsx 为路由表**

```tsx
import { Routes, Route } from 'react-router-dom'
import MainLayout from './layouts/MainLayout'
import UrlDownload from './pages/UrlDownload'
import Library from './pages/Library'
import Reader from './pages/Reader'
import Settings from './pages/Settings'

export default function App() {
  return (
    <Routes>
      <Route element={<MainLayout />}>
        <Route index element={<UrlDownload />} />
        <Route path="library" element={<Library />} />
        <Route path="reader/:id" element={<Reader />} />
        <Route path="settings" element={<Settings />} />
      </Route>
    </Routes>
  )
}
```
> `Reader` 在 Task 8 创建；为让本任务先编译通过，先创建一个占位 `src/renderer/pages/Reader.tsx`：`export default function Reader(){return <div className="p-6">阅读器</div>}`（Task 8 替换）。

- [ ] **Step 8: 改 main.tsx 挂载 Router + Antd + css**

使用 `HashRouter`（Electron 加载 file:// 时 BrowserRouter 的路径会出问题，Hash 路由最稳）。

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import { HashRouter } from 'react-router-dom'
import { ConfigProvider } from 'antd'
import zhCN from 'antd/locale/zh_CN'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ConfigProvider locale={zhCN}>
      <HashRouter>
        <App />
      </HashRouter>
    </ConfigProvider>
  </React.StrictMode>,
)
```

- [ ] **Step 9: 校验编译**

Run: `cd /Users/chenzhian/workspace/ai/wx-kit && npx tsc --noEmit -p tsconfig.json`
Expected: 无类型错误。

- [ ] **Step 10: 手动验证应用壳**

Run: `cd /Users/chenzhian/workspace/ai/wx-kit && npm run dev`
Expected: Electron 窗口打开，左侧出现「URL 下载 / 文章库 / 设置」三项菜单，点击切换内容区标题；无控制台报错。关闭窗口结束。

- [ ] **Step 11: 提交**

```bash
git add -A && git commit -m "feat(gui): add antd+tailwind+router app shell with nav"
```

---

### Task 2: SettingsService（TDD）

**Files:**
- Create: `electron/services/settings.ts`, `tests/electron/settings.test.ts`

- [ ] **Step 1: 写失败测试**（用临时目录注入存储位置）

```ts
// tests/electron/settings.test.ts
import { describe, it, expect, beforeEach } from 'vitest'
import { mkdtempSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { SettingsService } from '../../electron/services/settings'

describe('SettingsService', () => {
  let dir: string
  beforeEach(() => { dir = mkdtempSync(join(tmpdir(), 'wxk-set-')) })

  it('returns defaults when no file exists', async () => {
    const s = new SettingsService(dir, '/default/lib')
    const v = await s.get()
    expect(v).toEqual({ libraryRoot: '/default/lib', defaultFormats: ['md', 'html', 'meta'] })
  })

  it('persists and reloads saved settings', async () => {
    const s = new SettingsService(dir, '/default/lib')
    await s.save({ libraryRoot: '/custom', defaultFormats: ['md', 'pdf'] })
    const s2 = new SettingsService(dir, '/default/lib')
    expect(await s2.get()).toEqual({ libraryRoot: '/custom', defaultFormats: ['md', 'pdf'] })
  })

  it('merges partial save over existing', async () => {
    const s = new SettingsService(dir, '/default/lib')
    await s.save({ libraryRoot: '/custom', defaultFormats: ['md'] })
    await s.save({ defaultFormats: ['html'] })
    expect(await s.get()).toEqual({ libraryRoot: '/custom', defaultFormats: ['html'] })
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `cd /Users/chenzhian/workspace/ai/wx-kit && npx vitest run tests/electron/settings.test.ts`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现**

```ts
// electron/services/settings.ts
import { readFile, writeFile, mkdir } from 'node:fs/promises'
import { join } from 'node:path'
import type { DownloadFormat } from '../../src/core/types'

export interface AppSettings {
  libraryRoot: string
  defaultFormats: DownloadFormat[]
}

export class SettingsService {
  private path: string
  constructor(private storeDir: string, private defaultLibraryRoot: string) {
    this.path = join(storeDir, 'settings.json')
  }

  private defaults(): AppSettings {
    return { libraryRoot: this.defaultLibraryRoot, defaultFormats: ['md', 'html', 'meta'] }
  }

  async get(): Promise<AppSettings> {
    try {
      const raw = JSON.parse(await readFile(this.path, 'utf-8'))
      return { ...this.defaults(), ...raw }
    } catch (err) {
      if ((err as NodeJS.ErrnoException).code === 'ENOENT') return this.defaults()
      throw new Error(`settings file is corrupt at ${this.path} — delete it to reset`)
    }
  }

  async save(patch: Partial<AppSettings>): Promise<AppSettings> {
    const next = { ...(await this.get()), ...patch }
    await mkdir(this.storeDir, { recursive: true })
    await writeFile(this.path, JSON.stringify(next, null, 2), 'utf-8')
    return next
  }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run tests/electron/settings.test.ts`
Expected: PASS（3 用例）。

- [ ] **Step 5: 提交**

```bash
git add electron/services/settings.ts tests/electron/settings.test.ts
git commit -m "feat(electron): SettingsService with defaults and partial merge"
```

---

### Task 3: wxfile:// 协议（TDD 路径解析 + 注册）

阅读器要显示文章目录下的本地图片/HTML。注册一个自定义协议把 `wxfile://` 请求映射到库根目录内的文件，并禁止越界访问。

**Files:**
- Create: `electron/protocol.ts`, `tests/electron/protocol-resolve.test.ts`

- [ ] **Step 1: 写失败测试**（只测纯函数 resolveWxfilePath）

约定：`wxfile://host/<url-encoded-absolute-path>` 中，主机段忽略，pathname 是相对库根的、或我们直接用 pathname 作为相对 root 的子路径。为简单与安全，约定 URL 形如 `wxfile://local/<relPathFromRoot>`，解析时 join(root, decode(relPath)) 并校验仍在 root 内。

```ts
// tests/electron/protocol-resolve.test.ts
import { describe, it, expect } from 'vitest'
import { resolveWxfilePath } from '../../electron/protocol'

const ROOT = '/lib/root'

describe('resolveWxfilePath', () => {
  it('resolves a normal in-root file', () => {
    expect(resolveWxfilePath('wxfile://local/A/2026_t/index.html', ROOT))
      .toBe('/lib/root/A/2026_t/index.html')
  })
  it('decodes percent-encoded segments (chinese/space)', () => {
    expect(resolveWxfilePath('wxfile://local/' + encodeURIComponent('公众号') + '/a.png', ROOT))
      .toBe('/lib/root/公众号/a.png')
  })
  it('rejects path traversal outside root', () => {
    expect(resolveWxfilePath('wxfile://local/../../etc/passwd', ROOT)).toBeNull()
  })
  it('rejects absolute escape', () => {
    expect(resolveWxfilePath('wxfile://local/' + encodeURIComponent('/etc/passwd'), ROOT)).toBeNull()
  })
})
```
> 注：`encodeURIComponent` 不编码 `/`，所以路径里的 `/` 作为分隔符保留；中文/空格会被编码，解析时 decode。第 4 用例用 encodeURIComponent 把 `/etc/passwd` 整段编码成一个段，确保它被当作 root 下的子路径而非绝对路径——解析后应仍在 root 内，因此……改为更明确的越界用例见实现说明。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run tests/electron/protocol-resolve.test.ts`
Expected: FAIL。

- [ ] **Step 3: 实现**

```ts
// electron/protocol.ts
import { protocol, net } from 'electron'
import { resolve, sep } from 'node:path'
import { pathToFileURL } from 'node:url'

export const WXFILE_SCHEME = 'wxfile'

/** 把 wxfile://local/<relpath> 解析为 root 内的绝对路径；越界返回 null。 */
export function resolveWxfilePath(url: string, root: string): string | null {
  let u: URL
  try { u = new URL(url) } catch { return null }
  // pathname 形如 /A/2026_t/index.html（已含前导斜杠），逐段 decode
  const rel = u.pathname.replace(/^\/+/, '').split('/').map(decodeURIComponent).join('/')
  const resolvedRoot = resolve(root)
  const target = resolve(resolvedRoot, rel)
  if (target !== resolvedRoot && !target.startsWith(resolvedRoot + sep)) return null
  return target
}

/** 必须在 app ready 前调用：声明协议为可加载本地资源的特权协议。 */
export function registerWxfileScheme(): void {
  protocol.registerSchemesAsPrivileged([
    { scheme: WXFILE_SCHEME, privileges: { standard: true, secure: true, supportFetchAPI: true, stream: true } },
  ])
}

/** app ready 后调用：用 getRoot() 动态取当前库根目录。 */
export function handleWxfileProtocol(getRoot: () => string): void {
  protocol.handle(WXFILE_SCHEME, (req) => {
    const target = resolveWxfilePath(req.url, getRoot())
    if (!target) return new Response('forbidden', { status: 403 })
    return net.fetch(pathToFileURL(target).toString())
  })
}
```
> 实现说明（修正 Step 1 第 4 用例）：因 `encodeURIComponent('/etc/passwd')` → `%2Fetc%2Fpasswd` 是单段，decode 后含 `/`，`resolve(root, 'etc/passwd'...)` 实际仍在 root 内——所以该用例应断言它 **不为 null 且在 root 内**。把第 4 用例改为：
> ```ts
> it('keeps encoded slashes inside root (no escape)', () => {
>   const out = resolveWxfilePath('wxfile://local/' + encodeURIComponent('/etc/passwd'), ROOT)
>   expect(out).not.toBeNull()
>   expect(out!.startsWith('/lib/root')).toBe(true)
> })
> ```
> 真正的越界由第 3 用例（`../../`）覆盖。实现先写好后据此修正测试再跑绿。

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run tests/electron/protocol-resolve.test.ts`
Expected: PASS（4 用例，按上面修正后）。

- [ ] **Step 5: 提交**

```bash
git add electron/protocol.ts tests/electron/protocol-resolve.test.ts
git commit -m "feat(electron): wxfile protocol with root-contained path resolution"
```

---

### Task 4: core readArticleContent（TDD）

**Files:**
- Create: `src/core/read-article.ts`, `tests/core/read-article.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// tests/core/read-article.test.ts
import { describe, it, expect, beforeEach } from 'vitest'
import { mkdtempSync, mkdirSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { readArticleContent } from '../../src/core/read-article'

describe('readArticleContent', () => {
  let dir: string
  beforeEach(() => {
    dir = mkdtempSync(join(tmpdir(), 'wxk-read-'))
    writeFileSync(join(dir, 'content.md'), '---\ntitle: "T"\nsource: "u"\n---\n# T\n\n正文段落\n')
    writeFileSync(join(dir, 'index.html'), '<!doctype html><html><body>hi</body></html>')
  })

  it('reads md with frontmatter stripped', async () => {
    const out = await readArticleContent(dir, 'md')
    expect(out.startsWith('---')).toBe(false)
    expect(out).toContain('# T')
    expect(out).toContain('正文段落')
  })
  it('reads html raw', async () => {
    const out = await readArticleContent(dir, 'html')
    expect(out).toContain('<!doctype html>')
  })
  it('throws clear error when requested format file missing', async () => {
    const empty = mkdtempSync(join(tmpdir(), 'wxk-empty-'))
    await expect(readArticleContent(empty, 'md')).rejects.toThrow(/not found/i)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run tests/core/read-article.test.ts`
Expected: FAIL。

- [ ] **Step 3: 实现**

```ts
// src/core/read-article.ts
import { readFile } from 'node:fs/promises'
import { join } from 'node:path'

export type ReadableKind = 'md' | 'html'

const FILE_BY_KIND: Record<ReadableKind, string> = { md: 'content.md', html: 'index.html' }

/** 读取文章正文。md 去除 YAML frontmatter（开头 --- ... --- 块）。 */
export async function readArticleContent(dir: string, kind: ReadableKind): Promise<string> {
  const file = join(dir, FILE_BY_KIND[kind])
  let text: string
  try {
    text = await readFile(file, 'utf-8')
  } catch (err) {
    if ((err as NodeJS.ErrnoException).code === 'ENOENT') {
      throw new Error(`${kind} content not found in ${dir}`)
    }
    throw err
  }
  if (kind === 'md') {
    const m = text.match(/^---\n[\s\S]*?\n---\n/)
    if (m) text = text.slice(m[0].length)
  }
  return text
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run tests/core/read-article.test.ts`
Expected: PASS（3 用例）。

- [ ] **Step 5: 提交**

```bash
git add src/core/read-article.ts tests/core/read-article.test.ts
git commit -m "feat(core): readArticleContent (md frontmatter-stripped / html raw)"
```

---

### Task 5: IPC 桥 + preload + 主进程接线

把 core 能力暴露给渲染层。下载在主进程跑队列并把进度流式推回。

**Files:**
- Create: `electron/ipc.ts`, `src/renderer/api.ts`
- Modify: `electron/preload.ts`, `electron/main.ts`

- [ ] **Step 1: 创建共享 DTO 与 api 类型 `src/renderer/api.ts`**

```ts
// src/renderer/api.ts
import type { ArticleMeta, DownloadFormat, DownloadSummary, ProgressEvent } from '../core/types'
import type { AppSettings } from '../../electron/services/settings'
import type { ReadableKind } from '../core/read-article'

export interface WxApi {
  download(urls: string[], formats: DownloadFormat[]): Promise<DownloadSummary>
  onDownloadProgress(cb: (e: ProgressEvent) => void): () => void
  libraryList(): Promise<ArticleMeta[]>
  librarySearch(keyword: string): Promise<ArticleMeta[]>
  libraryRemove(id: string): Promise<void>
  readContent(dir: string, kind: ReadableKind): Promise<string>
  getSettings(): Promise<AppSettings>
  saveSettings(patch: Partial<AppSettings>): Promise<AppSettings>
  chooseDir(): Promise<string | null>
  reveal(path: string): Promise<void>
}

declare global {
  interface Window { api: WxApi }
}

export const api: WxApi = window.api
```

- [ ] **Step 2: 改 `electron/preload.ts` 暴露 window.api**

```ts
// electron/preload.ts
import { contextBridge, ipcRenderer } from 'electron'
import type { WxApi } from '../src/renderer/api'

const api: WxApi = {
  download: (urls, formats) => ipcRenderer.invoke('download', { urls, formats }),
  onDownloadProgress: (cb) => {
    const listener = (_e: unknown, ev: Parameters<typeof cb>[0]) => cb(ev)
    ipcRenderer.on('download:progress', listener)
    return () => { ipcRenderer.removeListener('download:progress', listener) }
  },
  libraryList: () => ipcRenderer.invoke('library:list'),
  librarySearch: (kw) => ipcRenderer.invoke('library:search', kw),
  libraryRemove: (id) => ipcRenderer.invoke('library:remove', id),
  readContent: (dir, kind) => ipcRenderer.invoke('library:readContent', { dir, kind }),
  getSettings: () => ipcRenderer.invoke('settings:get'),
  saveSettings: (patch) => ipcRenderer.invoke('settings:save', patch),
  chooseDir: () => ipcRenderer.invoke('dialog:chooseDir'),
  reveal: (path) => ipcRenderer.invoke('shell:reveal', path),
}

contextBridge.exposeInMainWorld('api', api)
```

- [ ] **Step 3: 创建 `electron/ipc.ts`**

```ts
// electron/ipc.ts
import { ipcMain, dialog, shell, BrowserWindow } from 'electron'
import type { DownloadFormat } from '../src/core/types'
import { fetchHtml, fetchBinary } from '../src/core/fetch-html'
import { Library } from '../src/core/library'
import { DownloadQueue } from '../src/core/download-queue'
import { downloadArticle } from '../src/core/download-article'
import { readArticleContent, type ReadableKind } from '../src/core/read-article'
import { SettingsService } from './services/settings'

export function registerIpc(settings: SettingsService): void {
  const libraryFor = async () => new Library((await settings.get()).libraryRoot)

  ipcMain.handle('settings:get', () => settings.get())
  ipcMain.handle('settings:save', (_e, patch) => settings.save(patch))

  ipcMain.handle('library:list', async () => (await libraryFor()).list())
  ipcMain.handle('library:search', async (_e, kw: string) => (await libraryFor()).search(kw))
  ipcMain.handle('library:remove', async (_e, id: string) => { await (await libraryFor()).remove(id) })
  ipcMain.handle('library:readContent', (_e, { dir, kind }: { dir: string; kind: ReadableKind }) =>
    readArticleContent(dir, kind))

  ipcMain.handle('dialog:chooseDir', async () => {
    const r = await dialog.showOpenDialog({ properties: ['openDirectory', 'createDirectory'] })
    return r.canceled ? null : r.filePaths[0]
  })
  ipcMain.handle('shell:reveal', (_e, path: string) => { shell.showItemInFolder(path) })

  ipcMain.handle('download', async (event, { urls, formats }: { urls: string[]; formats: DownloadFormat[] }) => {
    const { libraryRoot } = await settings.get()
    const library = new Library(libraryRoot)
    const deps = {
      fetchHtml, fetchBinary, BrowserWindowCtor: BrowserWindow,
      now: () => new Date().toISOString(), library, libraryRoot,
    }
    const queue = new DownloadQueue(
      (url) => downloadArticle(url, formats, deps),
      (ev) => event.sender.send('download:progress', ev),
    )
    return queue.run(urls)
  })
}
```

- [ ] **Step 4: 改 `electron/main.ts`** —— 注册协议、GUI 模式接线 IPC

在文件顶部 import：
```ts
import { app, BrowserWindow } from 'electron'
import path from 'node:path'
import { homedir } from 'node:os'
import { join } from 'node:path'
import { runCli } from '../src/cli'
import { registerWxfileScheme, handleWxfileProtocol } from './protocol'
import { registerIpc } from './ipc'
import { SettingsService } from './services/settings'
```
在 `main()` 之前、模块顶层调用（必须在 app ready 前声明协议）：
```ts
registerWxfileScheme()
```
把 GUI 分支改为接线 settings/ipc/protocol：
```ts
  // GUI 模式
  await app.whenReady()

  const settings = new SettingsService(app.getPath('userData'), join(app.getPath('documents'), 'wx-kit'))
  handleWxfileProtocol(async () => (await settings.get()).libraryRoot)
  registerIpc(settings)

  const win = new BrowserWindow({
    width: 1200, height: 800, title: 'wx-kit',
    webPreferences: { preload: path.join(__dirname, 'preload.js'), contextIsolation: true, nodeIntegration: false },
  })
  const devUrl = process.env.VITE_DEV_SERVER_URL
  if (devUrl) win.loadURL(devUrl)
  else win.loadFile(path.join(__dirname, '../dist/index.html'))

  app.on('window-all-closed', () => { if (process.platform !== 'darwin') app.quit() })
```
> 注意：`handleWxfileProtocol` 的 getRoot 是 async（settings.get 返回 Promise）。把 `protocol.ts` 的 `getRoot: () => string` 改为 `getRoot: () => string | Promise<string>` 并在 handler 内 `await getRoot()`。请相应更新 `electron/protocol.ts`（handler 改为 `const root = await getRoot()`）。CLI 分支完全不变。

- [ ] **Step 5: 校验编译 + 现有测试不回归**

Run:
```bash
cd /Users/chenzhian/workspace/ai/wx-kit
npx tsc --noEmit -p tsconfig.json && echo tsc-ok
npx vitest run 2>&1 | grep -E "Test Files|Tests "
```
Expected: tsc 无错；测试仍全绿（M1 50 + Task2 3 + Task3 4 + Task4 3 = 60）。

- [ ] **Step 6: 提交**

```bash
git add electron/ipc.ts electron/preload.ts electron/main.ts electron/protocol.ts src/renderer/api.ts
git commit -m "feat(electron): IPC bridge (download/library/settings/dialog) + protocol wiring"
```

---

### Task 6: URL 下载页

**Files:**
- Modify: `src/renderer/pages/UrlDownload.tsx`

- [ ] **Step 1: 实现页面**

多行 URL 输入 + 格式多选 + 下载按钮 + 进度（整体进度条 + 每条状态列表）。订阅 `onDownloadProgress`。

```tsx
import { useEffect, useState } from 'react'
import { Input, Checkbox, Button, Progress, List, Tag, message } from 'antd'
import { api } from '../api'
import type { DownloadFormat, DownloadItemResult, ProgressEvent } from '../../core/types'

const FORMATS: DownloadFormat[] = ['cover', 'md', 'html', 'pdf', 'meta']

export default function UrlDownload() {
  const [text, setText] = useState('')
  const [formats, setFormats] = useState<DownloadFormat[]>(['md', 'html', 'meta'])
  const [running, setRunning] = useState(false)
  const [progress, setProgress] = useState<ProgressEvent | null>(null)
  const [items, setItems] = useState<DownloadItemResult[]>([])

  useEffect(() => {
    api.getSettings().then(s => setFormats(s.defaultFormats)).catch(() => {})
    const off = api.onDownloadProgress(setProgress)
    return off
  }, [])

  const start = async () => {
    const urls = text.split(/\r?\n/).map(s => s.trim()).filter(Boolean)
    if (!urls.length) { message.warning('请输入至少一个 URL'); return }
    if (!formats.length) { message.warning('请至少选择一种格式'); return }
    setRunning(true); setItems([]); setProgress(null)
    try {
      const summary = await api.download(urls, formats)
      setItems(summary.items)
      message.success(`完成：成功 ${summary.succeeded} · 跳过 ${summary.skipped} · 失败 ${summary.failed}`)
    } catch (e) {
      message.error('下载出错：' + (e as Error).message)
    } finally {
      setRunning(false); setProgress(null)
    }
  }

  const pct = progress ? Math.round((progress.completed / Math.max(progress.total, 1)) * 100) : 0

  return (
    <div className="p-6" style={{ maxWidth: 820 }}>
      <h2>URL 下载</h2>
      <Input.TextArea value={text} onChange={e => setText(e.target.value)}
        placeholder="每行一个微信文章链接" autoSize={{ minRows: 4, maxRows: 10 }} disabled={running} />
      <div className="my-3">
        <Checkbox.Group options={FORMATS.map(f => ({ label: f, value: f }))}
          value={formats} onChange={v => setFormats(v as DownloadFormat[])} disabled={running} />
      </div>
      <Button type="primary" loading={running} onClick={start}>开始下载</Button>

      {running && progress && (
        <div className="mt-4">
          <Progress percent={pct} />
          <div style={{ color: '#888' }}>{progress.phase} · {progress.currentUrl}</div>
        </div>
      )}

      {items.length > 0 && (
        <List className="mt-4" size="small" bordered dataSource={items}
          renderItem={(it) => (
            <List.Item>
              <span style={{ flex: 1, overflow: 'hidden', textOverflow: 'ellipsis' }}>{it.url}</span>
              {it.skipped ? <Tag color="default">已存在</Tag>
                : it.ok ? <Tag color="success">成功</Tag>
                : <Tag color="error">失败：{it.error?.message}</Tag>}
            </List.Item>
          )} />
      )}
    </div>
  )
}
```

- [ ] **Step 2: 校验编译**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无错。

- [ ] **Step 3: 手动验证**（联网，需一个真实公众号文章 URL）

Run: `npm run dev`，在「URL 下载」粘贴 1-2 个真实链接，勾选 md/html/meta/cover/pdf，点开始。
Expected: 进度条推进、阶段文字变化；结束后每条显示成功/失败/已存在；message 汇总；文件落到库根目录（默认 `~/Documents/wx-kit`）。重复同链接显示「已存在」。

- [ ] **Step 4: 提交**

```bash
git add src/renderer/pages/UrlDownload.tsx
git commit -m "feat(gui): url download page with formats and live progress"
```

---

### Task 7: 文章库页

**Files:**
- Modify: `src/renderer/pages/Library.tsx`

- [ ] **Step 1: 实现页面**

搜索（按标题/文件名）+ 列表（公众号、标题、发布/下载时间、格式标记）+ 操作（阅读、在文件夹显示、删除带确认）。

```tsx
import { useEffect, useState } from 'react'
import { Input, Table, Tag, Space, Button, Popconfirm, message } from 'antd'
import { useNavigate } from 'react-router-dom'
import { api } from '../api'
import type { ArticleMeta } from '../../core/types'

export default function Library() {
  const [kw, setKw] = useState('')
  const [rows, setRows] = useState<ArticleMeta[]>([])
  const [loading, setLoading] = useState(false)
  const nav = useNavigate()

  const load = async (keyword = '') => {
    setLoading(true)
    try { setRows(keyword ? await api.librarySearch(keyword) : await api.libraryList()) }
    finally { setLoading(false) }
  }
  useEffect(() => { load() }, [])

  const del = async (id: string) => {
    await api.libraryRemove(id); message.success('已删除'); load(kw)
  }

  const columns = [
    { title: '标题', dataIndex: 'title', key: 'title', ellipsis: true },
    { title: '公众号', dataIndex: 'account', key: 'account', width: 140 },
    { title: '发布', dataIndex: 'publishTime', key: 'publishTime', width: 160 },
    { title: '格式', dataIndex: 'formats', key: 'formats', width: 200,
      render: (fs: string[]) => fs.map(f => <Tag key={f}>{f}</Tag>) },
    { title: '操作', key: 'op', width: 240, render: (_: unknown, r: ArticleMeta) => (
      <Space>
        <Button size="small" disabled={!r.formats.includes('md') && !r.formats.includes('html')}
          onClick={() => nav(`/reader/${encodeURIComponent(r.id)}`)}>阅读</Button>
        <Button size="small" onClick={() => api.reveal(r.dir)}>文件夹</Button>
        <Popconfirm title="删除该文章？将同时删除磁盘文件" okText="删除" cancelText="取消" onConfirm={() => del(r.id)}>
          <Button size="small" danger>删除</Button>
        </Popconfirm>
      </Space>
    ) },
  ]

  return (
    <div className="p-6">
      <h2>文章库</h2>
      <Input.Search className="mb-3" allowClear placeholder="按标题搜索"
        value={kw} onChange={e => setKw(e.target.value)} onSearch={(v) => load(v)} style={{ maxWidth: 360 }} />
      <Table rowKey="id" loading={loading} columns={columns} dataSource={rows} size="middle" pagination={{ pageSize: 20 }} />
    </div>
  )
}
```
> 阅读路由用 `id`；Reader 页会先 `libraryList` 找到该 id 的 meta 取 `dir`。为减少往返，也可改成把 dir 通过路由 state 传递；本计划用 id + 列表查找（简单、单一数据源）。

- [ ] **Step 2: 校验编译 + 手动验证**

Run: `npx tsc --noEmit -p tsconfig.json`，然后 `npm run dev`。
Expected: 文章库列出已下载文章；搜索按标题过滤；「文件夹」打开 Finder 定位；「删除」二次确认后从列表与磁盘移除；无 md/html 的行「阅读」禁用。

- [ ] **Step 3: 提交**

```bash
git add src/renderer/pages/Library.tsx
git commit -m "feat(gui): library page with search, reveal, delete"
```

---

### Task 8: 内置阅读器（md / html）

**Files:**
- Modify (替换占位): `src/renderer/pages/Reader.tsx`

- [ ] **Step 1: 实现阅读器**

根据 id 找到 meta，读取内容。md 用 react-markdown 渲染，相对图片改写为 `wxfile://local/<相对库根的文章子路径>/...`；html 用 `<iframe>` 加载 `wxfile://local/<...>/index.html`。

需要把文章绝对 `dir` 转成相对库根的子路径来拼 wxfile URL。最简单：让 IPC 暴露库根，或在前端用 settings.libraryRoot 计算相对路径。本步用 `getSettings().libraryRoot` 计算相对路径。

```tsx
import { useEffect, useMemo, useState } from 'react'
import { useParams, useNavigate } from 'react-router-dom'
import { Segmented, Button, Spin, Empty } from 'antd'
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'
import { api } from '../api'
import type { ArticleMeta } from '../../core/types'

function toWxfileBase(libraryRoot: string, dir: string): string {
  // dir 在 libraryRoot 之下；取相对子路径，按 / 编码每段
  let rel = dir.startsWith(libraryRoot) ? dir.slice(libraryRoot.length) : dir
  rel = rel.replace(/^[/\\]+/, '').split(/[/\\]/).map(encodeURIComponent).join('/')
  return `wxfile://local/${rel}`
}

export default function Reader() {
  const { id } = useParams()
  const nav = useNavigate()
  const [meta, setMeta] = useState<ArticleMeta | null>(null)
  const [root, setRoot] = useState('')
  const [kind, setKind] = useState<'md' | 'html'>('md')
  const [md, setMd] = useState('')
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    (async () => {
      const [list, s] = await Promise.all([api.libraryList(), api.getSettings()])
      setRoot(s.libraryRoot)
      const m = list.find(a => a.id === decodeURIComponent(id ?? '')) ?? null
      setMeta(m)
      if (m) {
        const has = (k: 'md' | 'html') => m.formats.includes(k)
        setKind(has('md') ? 'md' : 'html')
      }
      setLoading(false)
    })()
  }, [id])

  const base = useMemo(() => (meta && root ? toWxfileBase(root, meta.dir) : ''), [meta, root])

  useEffect(() => {
    if (meta && kind === 'md') api.readContent(meta.dir, 'md').then(setMd).catch(() => setMd(''))
  }, [meta, kind])

  if (loading) return <div className="p-6"><Spin /></div>
  if (!meta) return <div className="p-6"><Empty description="未找到文章" /></div>

  return (
    <div className="p-6" style={{ height: '100%', display: 'flex', flexDirection: 'column' }}>
      <div className="mb-3" style={{ display: 'flex', gap: 12, alignItems: 'center' }}>
        <Button onClick={() => nav('/library')}>返回</Button>
        <b style={{ flex: 1 }}>{meta.title}</b>
        <Segmented value={kind} onChange={(v) => setKind(v as 'md' | 'html')}
          options={[
            { label: 'Markdown', value: 'md', disabled: !meta.formats.includes('md') },
            { label: 'HTML', value: 'html', disabled: !meta.formats.includes('html') },
          ]} />
      </div>

      {kind === 'html' ? (
        <iframe title="article" sandbox="allow-same-origin" style={{ flex: 1, border: '1px solid #eee' }}
          src={`${base}/index.html`} />
      ) : (
        <div style={{ flex: 1, overflow: 'auto', maxWidth: 760 }}>
          <ReactMarkdown remarkPlugins={[remarkGfm]}
            components={{
              img: ({ src = '', ...rest }) => {
                const resolved = src.startsWith('images/') ? `${base}/${src.split('/').map(encodeURIComponent).join('/')}` : src
                return <img src={resolved} style={{ maxWidth: '100%' }} {...rest} />
              },
            }}>
            {md}
          </ReactMarkdown>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 校验编译**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无错。

- [ ] **Step 3: 手动验证**

Run: `npm run dev`，先在 URL 下载页下一篇含图片的文章（勾 md+html+cover），到文章库点「阅读」。
Expected: 默认 Markdown 视图渲染正文，**图片能显示**（走 wxfile 协议）；切到 HTML 视图，iframe 里展示自包含页面、图片显示；切换 md/html 流畅；无 md/html 的格式按钮禁用。

- [ ] **Step 4: 提交**

```bash
git add src/renderer/pages/Reader.tsx
git commit -m "feat(gui): built-in reader for md (react-markdown) and html (iframe via wxfile)"
```

---

### Task 9: 设置页

**Files:**
- Modify: `src/renderer/pages/Settings.tsx`

- [ ] **Step 1: 实现**

库根目录（显示 + 选择目录按钮）+ 默认格式多选 + 保存。

```tsx
import { useEffect, useState } from 'react'
import { Input, Button, Checkbox, Space, message } from 'antd'
import { api } from '../api'
import type { AppSettings } from '../../../electron/services/settings'
import type { DownloadFormat } from '../../core/types'

const FORMATS: DownloadFormat[] = ['cover', 'md', 'html', 'pdf', 'meta']

export default function Settings() {
  const [s, setS] = useState<AppSettings | null>(null)

  useEffect(() => { api.getSettings().then(setS) }, [])

  const choose = async () => {
    const dir = await api.chooseDir()
    if (dir && s) setS({ ...s, libraryRoot: dir })
  }
  const save = async () => {
    if (!s) return
    await api.saveSettings(s); message.success('已保存')
  }

  if (!s) return <div className="p-6">加载中…</div>
  return (
    <div className="p-6" style={{ maxWidth: 640 }}>
      <h2>设置</h2>
      <div className="mb-2">文章库根目录</div>
      <Space.Compact style={{ width: '100%' }}>
        <Input value={s.libraryRoot} readOnly />
        <Button onClick={choose}>选择目录</Button>
      </Space.Compact>
      <div className="mt-4 mb-2">默认下载格式</div>
      <Checkbox.Group options={FORMATS.map(f => ({ label: f, value: f }))}
        value={s.defaultFormats} onChange={(v) => setS({ ...s, defaultFormats: v as DownloadFormat[] })} />
      <div className="mt-6"><Button type="primary" onClick={save}>保存</Button></div>
    </div>
  )
}
```

- [ ] **Step 2: 校验编译 + 手动验证**

Run: `npx tsc --noEmit -p tsconfig.json`，`npm run dev`。
Expected: 显示当前库根目录与默认格式；改目录、改格式、保存后 message 提示；重启应用后设置仍在；URL 下载页默认勾选项跟随此处的默认格式。

- [ ] **Step 3: 提交**

```bash
git add src/renderer/pages/Settings.tsx
git commit -m "feat(gui): settings page for library root and default formats"
```

---

### Task 10: 端到端验证（手动）

完整跑一遍 GUI 主流程。

- [ ] **Step 1: 全量自动化测试回归**

Run: `cd /Users/chenzhian/workspace/ai/wx-kit && npx vitest run 2>&1 | grep -E "Test Files|Tests " && npx tsc --noEmit -p tsconfig.json && echo tsc-ok`
Expected: 全绿（≥60），tsc 干净。

- [ ] **Step 2: 构建产物**

Run: `npx vite build`
Expected: 生成 dist/ + dist-electron/main.js + preload.js，无报错。

- [ ] **Step 3: GUI 主流程**（`npm run dev`）

依次验证：
- 设置页改默认格式 → 保存。
- URL 下载页粘贴 2 个真实链接（含图）→ 默认勾选项已是设置里的格式 → 下载 → 进度推进 → 汇总成功。
- 文章库出现新文章；按标题搜索能过滤；「文件夹」打开 Finder。
- 「阅读」打开阅读器：md 视图图片正常、切 HTML 视图图片正常。
- 「删除」二次确认后从库与磁盘移除。
- 重复下载同链接 → 列表/汇总显示「已存在」。
Expected: 全程无控制台报错，体验顺畅。

- [ ] **Step 4: 提交（如期间有修复）**

```bash
git add -A && git commit -m "test: verify m2 gui end-to-end (manual)" --allow-empty
```

---

## 自检（写计划者执行，已完成）

**Spec 覆盖（对照 PRD M2 相关需求）：**
- F1 GUI（多行 URL + 五格式可选 + 实时进度）→ Task 6（默认格式来自设置 Task 9）。
- F3 文章库（列表/按文件名·标题搜索/删除连带磁盘/在文件夹显示）→ Task 7（IPC Task 5，core 删除来自 M1 Library）。
- F3 内置阅读器（md/html）→ Task 8（图片本地化显示走 wxfile 协议 Task 3）。
- §8.4 进度（download-queue 单一来源 → IPC 流式推 → GUI 进度条）→ Task 5/6。
- 设置（库根目录可配置默认用户文档目录、默认格式）→ Task 2/9。
- 架构（渲染层不碰 core，全走 IPC；core 复用 M1）→ Task 5。
- **范围外**：公众号批量/登录（M3）、打包跨平台（M4）。

**占位符扫描：** 无 TBD/TODO；可测接缝（settings/read-article/protocol-resolve）给完整 TDD 代码；React 页面给完整代码 + 手动验证步骤。Task 3 Step 1 的第 4 用例在 Step 3 实现说明里已给出修正版本，执行者据此落地。

**类型一致性：** `WxApi`（api.ts）为 preload 与 renderer 唯一契约；`AppSettings`/`ArticleMeta`/`DownloadFormat`/`DownloadSummary`/`ProgressEvent`/`ReadableKind` 全程一致；IPC channel 名（download / download:progress / library:* / settings:* / dialog:chooseDir / shell:reveal / library:readContent）在 preload 与 ipc.ts 两侧逐一对应。
```
